<div class="min-h-screen bg-gray-900 py-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- En-tête de la boutique -->
    <div class="text-center mb-12">
      <h1 class="text-4xl font-bold text-white mb-4">🛍️ Boutique</h1>
      <p class="text-xl text-gray-300">Découvrez nos offres exclusives et débloquez de nouveaux contenus</p>
      
      <!-- Affichage du solde de points -->
      <div class="mt-6 inline-flex items-center px-6 py-3 bg-gradient-to-r from-purple-600 to-blue-600 rounded-full">
        <span class="text-2xl mr-2">💰</span>
        <span class="text-white font-semibold text-lg">Votre solde : <%= @user_points %> points</span>
      </div>
    </div>

    <!-- Section Acheter des points -->
    <div class="mb-16">
      <div class="flex items-center mb-8">
        <h2 class="text-3xl font-bold text-white">Acheter des points</h2>
        <span class="ml-3 text-3xl">💰</span>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <% @point_packs.each_with_index do |pack, index| %>
          <div class="rounded-xl p-6 border border-gray-700 hover:border-purple-500 transition-all duration-300 transform hover:scale-105 overflow-hidden relative" 
               style="background-image: url('/assets/store/<%= pack[:points] <= 500 ? 'lovepik.png' : 'trove_of_coins.png' %>'); background-size: cover; background-position: center;">
            <!-- Overlay gradient -->
            <div class="absolute inset-0 bg-gradient-to-br from-purple-600/60 to-blue-600/60 rounded-xl"></div>
            
            <div class="relative z-10 text-center">
              <h3 class="text-xl font-semibold text-white mb-2"><%= pack[:name] %></h3>
              <div class="text-3xl font-bold text-purple-400 mb-2"><%= pack[:price] %> €</div>
              <p class="text-gray-300 text-sm mb-4"><%= pack[:description] %></p>
              
              <%= button_to buy_points_store_path(pack_id: index), 
                          method: :post, 
                          class: "w-full bg-gradient-to-r from-purple-600 to-blue-600 hover:from-purple-700 hover:to-blue-700 text-white font-semibold py-3 px-6 rounded-lg transition-all duration-300 transform hover:scale-105 relative z-20" do %>
                <span class="flex items-center justify-center">
                  <span class="mr-2">🛒</span>
                  Acheter
                </span>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Section Acheter des abonnements -->
    <div class="mb-16">
      <div class="flex items-center mb-8">
        <h2 class="text-3xl font-bold text-white">Acheter des abonnements</h2>
        <span class="ml-3 text-3xl">🎟️</span>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% @subscriptions.each do |subscription| %>
          <div class="rounded-xl p-8 border border-yellow-500 hover:border-yellow-400 transition-all duration-300 transform hover:scale-105 overflow-hidden relative"
               style="background-image: url('/assets/store/6070880.jpg'); background-size: cover; background-position: center;">
            <!-- Overlay gradient -->
            <div class="absolute inset-0 bg-gradient-to-br from-yellow-600/50 to-orange-600/50 rounded-xl"></div>
            
            <div class="relative z-10 text-center">
              <h3 class="text-2xl font-bold text-white mb-2">Abonnement <%= subscription[:name] %></h3>
              <div class="text-4xl font-bold text-white mb-4"><%= subscription[:price] %> €</div>
              <p class="text-yellow-100 text-sm mb-6"><%= subscription[:description] %></p>
              
              <%= button_to buy_subscription_store_path(subscription_type: subscription[:name].downcase), 
                          method: :post, 
                          class: "w-full bg-white text-orange-600 font-bold py-3 px-6 rounded-lg hover:bg-gray-100 transition-all duration-300 transform hover:scale-105 relative z-20" do %>
                <span class="flex items-center justify-center">
                  <span class="mr-2">🎟️</span>
                  Acheter <%= subscription[:price] %> €
                </span>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Section Acheter des playlists -->
    <div class="mb-16">
      <div class="flex items-center mb-8">
        <h2 class="text-3xl font-bold text-white">Acheter des playlists</h2>
        <span class="ml-3 text-3xl">🎶</span>
      </div>
      
      <% if @premium_playlists.any? %>
        <div class="playlists-grid">
          <% @premium_playlists.each do |playlist| %>
            <% is_unlocked = @unlocked_playlists.include?(playlist) %>
            <div class="playlist-item premium-playlist" data-category="<%= playlist.category || 'Premium' %>">
              <%= render 'store_playlist_card', playlist: playlist, is_unlocked: is_unlocked %>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="text-center py-12">
          <div class="text-6xl mb-4">🎵</div>
          <p class="text-gray-400 text-lg">Aucune playlist premium disponible pour le moment.</p>
        </div>
      <% end %>
    </div>

    <% if flash[:notice] %>
      <div class="bg-green-500 text-white p-4 rounded-lg mb-8 max-w-2xl mx-auto flex flex-col items-center gap-4">
        <span><%= flash[:notice].html_safe %></span>
        <%= link_to 'Retour aux playlists', playlists_path, class: 'inline-flex items-center px-6 py-3 bg-gradient-to-r from-purple-600 to-blue-600 hover:from-purple-700 hover:to-blue-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:scale-105' %>
      </div>
    <% end %>
  </div>
</div>

<style>
  @keyframes gradient {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }
  
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  /* Styles pour les z-index personnalisés */
  .z-45 {
    z-index: 45 !important;
  }
  
  .z-50 {
    z-index: 50 !important;
  }
  
  .z-60 {
    z-index: 60 !important;
  }
  
  .z-70 {
    z-index: 70 !important;
  }
  
  /* Style pour les boutons Jouer */
  .play-button {
    position: absolute;
    bottom: 1rem;
    left: 50%;
    transform: translateX(-50%);
    z-index: 50 !important;
    background: linear-gradient(to right, #2563eb, #7c3aed);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    font-weight: 600;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    transition: all 0.2s;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }
  
  .play-button:hover {
    background: linear-gradient(to right, #1d4ed8, #6d28d9);
    transform: translateX(-50%) scale(1.05);
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
  }
  
  /* Style pour les liens cliquables */
  .clickable-card {
    position: absolute;
    inset: 0;
    z-index: 40;
    cursor: pointer;
    background: transparent;
  }
  
  /* Styles uniformes pour les cartes de playlists */
  .playlists-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2rem;
  }
  
  .playlist-card-uniform {
    position: relative;
    background: #1f2937;
    border-radius: 0.75rem;
    overflow: hidden;
    transition: all 0.3s ease;
    border: 1px solid #374151;
    height: 100%;
    display: flex;
    flex-direction: column;
  }
  
  .playlist-card-uniform:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    border-color: #6b7280;
  }
  
  .playlist-image-uniform {
    position: relative;
    width: 100%;
    height: 200px;
    overflow: hidden;
  }
  
  .playlist-image-uniform img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }
  
  .playlist-card-uniform:hover .playlist-image-uniform img {
    transform: scale(1.05);
  }
  
  .playlist-overlay-uniform {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.8);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    color: white;
    z-index: 20;
  }
  
  .playlist-overlay-uniform svg {
    width: 3rem;
    height: 3rem;
    margin-bottom: 0.5rem;
    opacity: 0.8;
  }
  
  .overlay-title {
    font-size: 1.125rem;
    font-weight: 600;
    margin-bottom: 0.25rem;
  }
  
  .overlay-subtitle {
    font-size: 0.875rem;
    opacity: 0.8;
  }
  
  .playlist-text-uniform {
    padding: 1rem;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
  }
  
  .playlist-title-uniform {
    font-size: 1.125rem;
    font-weight: 600;
    color: white;
    margin-bottom: 0.5rem;
    line-height: 1.4;
  }
  
  .playlist-tags-uniform {
    display: flex;
    flex-wrap: wrap;
    gap: 0.25rem;
    margin-bottom: 0.75rem;
  }
  
  .playlist-tag-uniform {
    font-size: 0.75rem;
    font-weight: 500;
    padding: 0.25rem 0.5rem;
    border-radius: 0.375rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  
  .playlist-description-uniform {
    color: #9ca3af;
    font-size: 0.875rem;
    line-height: 1.5;
    margin-bottom: 0.75rem;
    flex-grow: 1;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  .playlist-footer-uniform {
    color: #6b7280;
    font-size: 0.875rem;
    margin-top: auto;
  }
  
  /* Styles spécifiques pour les types de playlists */
  .premium-playlist .playlist-card-uniform {
    border-color: #f59e0b;
  }
  
  .premium-playlist .playlist-card-uniform:hover {
    border-color: #fbbf24;
    box-shadow: 0 20px 25px -5px rgba(245, 158, 11, 0.1), 0 10px 10px -5px rgba(245, 158, 11, 0.04);
  }
  
  .exclusive-playlist .playlist-card-uniform {
    border-color: #8b5cf6;
  }
  
  .exclusive-playlist .playlist-card-uniform:hover {
    border-color: #a78bfa;
    box-shadow: 0 20px 25px -5px rgba(139, 92, 246, 0.1), 0 10px 10px -5px rgba(139, 92, 246, 0.04);
  }
  
  .standard-playlist .playlist-card-uniform {
    border-color: #374151;
  }
  
  .standard-playlist .playlist-card-uniform:hover {
    border-color: #4b5563;
  }
  
  /* Animations et effets visuels améliorés */
  .transform {
    transition: all 0.3s ease;
  }
  
  .hover\:scale-105:hover {
    transform: scale(1.05);
  }
  
  /* Effet de brillance sur les cartes avec images */
  .overflow-hidden {
    position: relative;
  }
  
  .overflow-hidden::after {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
    transition: left 0.5s;
    pointer-events: none;
    z-index: 5;
  }
  
  .overflow-hidden:hover::after {
    left: 100%;
  }
  
  /* Effet de brillance sur les boutons */
  .bg-gradient-to-r {
    position: relative;
    overflow: hidden;
  }
  
  .bg-gradient-to-r::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
    transition: left 0.5s;
  }
  
  .bg-gradient-to-r:hover::before {
    left: 100%;
  }
  
  /* Amélioration de l'opacité des images de fond */
  .opacity-15 {
    opacity: 0.15;
  }
  
  .opacity-20 {
    opacity: 0.2;
  }
  
  .opacity-30 {
    opacity: 0.3;
  }
  
  .opacity-35 {
    opacity: 0.35;
  }
  
  .opacity-40 {
    opacity: 0.4;
  }
  
  .opacity-50 {
    opacity: 0.5;
  }
  
  .opacity-60 {
    opacity: 0.6;
  }
  
  .opacity-70 {
    opacity: 0.7;
  }
  
  /* Effet de pulsation pour les cartes VIP */
  .bg-gradient-to-br.from-yellow-600 {
    animation: pulse-glow 2s ease-in-out infinite alternate;
  }
  
  @keyframes pulse-glow {
    from {
      box-shadow: 0 0 20px rgba(245, 158, 11, 0.3);
    }
    to {
      box-shadow: 0 0 30px rgba(245, 158, 11, 0.6);
    }
  }
  
  /* Effet de rotation subtile sur les images de fond au hover */
  .overflow-hidden:hover .bg-cover {
    transform: scale(1.1) rotate(1deg);
    transition: transform 0.3s ease;
  }
  
  /* Amélioration des transitions */
  .transition-all {
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  /* Effet de profondeur sur les cartes */
  .bg-gray-800 {
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  }
  
  .bg-gray-800:hover {
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  }
</style> 