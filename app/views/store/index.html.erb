<div class="min-h-screen bg-gray-900 py-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- En-tête de la boutique -->
    <div class="text-center mb-12">
      <h1 class="text-4xl font-bold text-white mb-4">🛍️ Boutique</h1>
      <p class="text-xl text-gray-300">Découvrez nos offres exclusives et débloquez de nouveaux contenus</p>
      
      <!-- Affichage du solde de points -->
      <div class="mt-6 inline-flex items-center px-6 py-3 bg-gradient-to-r from-purple-600 to-blue-600 rounded-full">
        <span class="text-2xl mr-2">💰</span>
        <span class="text-white font-semibold text-lg">Votre solde : <%= @user_points %> points</span>
      </div>
    </div>

    <!-- Section Acheter des points -->
    <div class="mb-16">
      <div class="flex items-center mb-8">
        <h2 class="text-3xl font-bold text-white">Acheter des points</h2>
        <span class="ml-3 text-3xl">💰</span>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <% @point_packs.each_with_index do |pack, index| %>
          <div class="bg-gray-800 rounded-xl p-6 border border-gray-700 hover:border-purple-500 transition-all duration-300 transform hover:scale-105 overflow-hidden relative">
            <!-- Fond gradient avec image -->
            <div class="absolute inset-0 bg-gradient-to-br from-purple-600/30 to-blue-600/30 rounded-xl"></div>
            <div class="absolute inset-0 bg-cover bg-center bg-no-repeat rounded-xl opacity-15" 
                 style="background-image: url('<%= asset_path("store/#{pack[:points] <= 500 ? 'lovepik.png' : 'trove_of_coins.png'}") %>');"></div>
            
            <div class="relative z-10 text-center">
              <h3 class="text-xl font-semibold text-white mb-2"><%= pack[:name] %></h3>
              <div class="text-3xl font-bold text-purple-400 mb-2"><%= pack[:price] %> €</div>
              <p class="text-gray-400 text-sm mb-4"><%= pack[:description] %></p>
              
              <%= button_to buy_points_store_path(pack_id: index), 
                          method: :post, 
                          class: "w-full bg-gradient-to-r from-purple-600 to-blue-600 hover:from-purple-700 hover:to-blue-700 text-white font-semibold py-3 px-6 rounded-lg transition-all duration-300 transform hover:scale-105 relative z-20" do %>
                <span class="flex items-center justify-center">
                  <span class="mr-2">🛒</span>
                  Acheter
                </span>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Section Acheter des abonnements -->
    <div class="mb-16">
      <div class="flex items-center mb-8">
        <h2 class="text-3xl font-bold text-white">Acheter des abonnements</h2>
        <span class="ml-3 text-3xl">🎟️</span>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% @subscriptions.each do |subscription| %>
          <div class="bg-gradient-to-br from-yellow-600 to-orange-600 rounded-xl p-8 border border-yellow-500 hover:border-yellow-400 transition-all duration-300 transform hover:scale-105 overflow-hidden relative">
            <!-- Fond gradient avec image VIP -->
            <div class="absolute inset-0 bg-gradient-to-br from-yellow-600/40 to-orange-600/40 rounded-xl"></div>
            <div class="absolute inset-0 bg-cover bg-center bg-no-repeat rounded-xl opacity-20" 
                 style="background-image: url('<%= asset_path("store/vip_recompense.png") %>');"></div>
            
            <div class="relative z-10 text-center">
              <h3 class="text-2xl font-bold text-white mb-2">Abonnement <%= subscription[:name] %></h3>
              <div class="text-4xl font-bold text-white mb-4"><%= subscription[:price] %> €</div>
              <p class="text-yellow-100 text-sm mb-6"><%= subscription[:description] %></p>
              
              <%= button_to buy_subscription_store_path(subscription_type: subscription[:name].downcase), 
                          method: :post, 
                          class: "w-full bg-white text-orange-600 font-bold py-3 px-6 rounded-lg hover:bg-gray-100 transition-all duration-300 transform hover:scale-105 relative z-20" do %>
                <span class="flex items-center justify-center">
                  <span class="mr-2">🎟️</span>
                  Acheter <%= subscription[:price] %> €
                </span>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Section Acheter des playlists -->
    <div class="mb-16">
      <div class="flex items-center mb-8">
        <h2 class="text-3xl font-bold text-white">Acheter des playlists</h2>
        <span class="ml-3 text-3xl">🎶</span>
      </div>
      
      <% if @premium_playlists.any? %>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <% @premium_playlists.each do |playlist| %>
            <div class="bg-gray-800 rounded-xl p-6 border border-gray-700 hover:border-green-500 transition-all duration-300 transform hover:scale-105 overflow-hidden relative">
              <!-- Fond gradient avec image de playlist -->
              <div class="absolute inset-0 bg-gradient-to-br from-green-600/30 to-emerald-600/30 rounded-xl"></div>
              <div class="absolute inset-0 bg-cover bg-center bg-no-repeat rounded-xl opacity-15" 
                   style="background-image: url('<%= asset_path("store/lovepik.png") %>');"></div>
              
              <div class="relative z-10 text-center">
                <h3 class="text-xl font-semibold text-white mb-2"><%= playlist.title %></h3>
                <p class="text-gray-400 text-sm mb-4"><%= truncate(playlist.description, length: 100) %></p>
                
                <div class="flex items-center justify-center mb-4">
                  <span class="text-2xl mr-2">💰</span>
                  <span class="text-green-400 font-bold text-lg"><%= playlist.points_required %> points</span>
                </div>
                
                <%= link_to buy_playlist_store_path(playlist), 
                          class: "w-full bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white font-semibold py-3 px-6 rounded-lg transition-all duration-300 transform hover:scale-105 inline-block text-center relative z-20" do %>
                  <span class="flex items-center justify-center">
                    <span class="mr-2">🛒</span>
                    Acheter
                  </span>
                <% end %>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="text-center py-12">
          <div class="text-6xl mb-4">🎵</div>
          <p class="text-gray-400 text-lg">Aucune playlist premium disponible pour le moment.</p>
        </div>
      <% end %>
    </div>
  </div>
</div>

<style>
  /* Animations et effets visuels améliorés */
  .transform {
    transition: all 0.3s ease;
  }
  
  .hover\:scale-105:hover {
    transform: scale(1.05);
  }
  
  /* Effet de brillance sur les cartes avec images */
  .overflow-hidden {
    position: relative;
  }
  
  .overflow-hidden::after {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
    transition: left 0.5s;
    pointer-events: none;
    z-index: 5;
  }
  
  .overflow-hidden:hover::after {
    left: 100%;
  }
  
  /* Effet de brillance sur les boutons */
  .bg-gradient-to-r {
    position: relative;
    overflow: hidden;
  }
  
  .bg-gradient-to-r::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
    transition: left 0.5s;
  }
  
  .bg-gradient-to-r:hover::before {
    left: 100%;
  }
  
  /* Amélioration de l'opacité des images de fond */
  .opacity-15 {
    opacity: 0.15;
  }
  
  .opacity-20 {
    opacity: 0.2;
  }
  
  /* Effet de pulsation pour les cartes VIP */
  .bg-gradient-to-br.from-yellow-600 {
    animation: pulse-glow 2s ease-in-out infinite alternate;
  }
  
  @keyframes pulse-glow {
    from {
      box-shadow: 0 0 20px rgba(245, 158, 11, 0.3);
    }
    to {
      box-shadow: 0 0 30px rgba(245, 158, 11, 0.6);
    }
  }
  
  /* Effet de rotation subtile sur les images de fond au hover */
  .overflow-hidden:hover .bg-cover {
    transform: scale(1.1) rotate(1deg);
    transition: transform 0.3s ease;
  }
  
  /* Amélioration des transitions */
  .transition-all {
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  /* Effet de profondeur sur les cartes */
  .bg-gray-800 {
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  }
  
  .bg-gray-800:hover {
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  }
</style> 