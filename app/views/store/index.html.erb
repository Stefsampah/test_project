<div class="min-h-screen bg-gray-900 py-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- En-tête de la boutique -->
    <div class="text-center mb-12">
      <h1 class="text-4xl font-bold text-white mb-4">🛍️ Boutique</h1>
      <p class="text-xl text-gray-300">Découvrez nos offres exclusives et débloquez de nouveaux contenus</p>
      
      <!-- Affichage du solde de points -->
      <div class="mt-6 inline-flex items-center px-6 py-3 bg-gradient-to-r from-purple-600 to-blue-600 rounded-full">
        <span class="text-2xl mr-2">💰</span>
        <span class="text-white font-semibold text-lg">Votre solde : <%= @user_points %> points</span>
      </div>
    </div>

    <!-- Section Acheter des points -->
    <div class="mb-16">
      <div class="flex items-center mb-8">
        <h2 class="text-3xl font-bold text-white">Acheter des points</h2>
        <span class="ml-3 text-3xl">💰</span>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <% @point_packs.each_with_index do |pack, index| %>
          <div class="bg-gray-800 rounded-xl p-6 border border-gray-700 hover:border-purple-500 transition-all duration-300 transform hover:scale-105">
            <div class="text-center">
              <h3 class="text-xl font-semibold text-white mb-2"><%= pack[:name] %></h3>
              <div class="text-3xl font-bold text-purple-400 mb-2"><%= pack[:price] %> €</div>
              <p class="text-gray-400 text-sm mb-4"><%= pack[:description] %></p>
              
              <%= button_to buy_points_store_path(pack_id: index), 
                          method: :post, 
                          class: "w-full bg-gradient-to-r from-purple-600 to-blue-600 hover:from-purple-700 hover:to-blue-700 text-white font-semibold py-3 px-6 rounded-lg transition-all duration-300 transform hover:scale-105" do %>
                <span class="flex items-center justify-center">
                  <span class="mr-2">🛒</span>
                  Acheter
                </span>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Section Acheter des abonnements -->
    <div class="mb-16">
      <div class="flex items-center mb-8">
        <h2 class="text-3xl font-bold text-white">Acheter des abonnements</h2>
        <span class="ml-3 text-3xl">🎟️</span>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% @subscriptions.each do |subscription| %>
          <div class="bg-gradient-to-br from-yellow-600 to-orange-600 rounded-xl p-8 border border-yellow-500 hover:border-yellow-400 transition-all duration-300 transform hover:scale-105">
            <div class="text-center">
              <h3 class="text-2xl font-bold text-white mb-2">Abonnement <%= subscription[:name] %></h3>
              <div class="text-4xl font-bold text-white mb-4"><%= subscription[:price] %> €</div>
              <p class="text-yellow-100 text-sm mb-6"><%= subscription[:description] %></p>
              
              <%= button_to buy_subscription_store_path(subscription_type: subscription[:name].downcase), 
                          method: :post, 
                          class: "w-full bg-white text-orange-600 font-bold py-3 px-6 rounded-lg hover:bg-gray-100 transition-all duration-300 transform hover:scale-105" do %>
                <span class="flex items-center justify-center">
                  <span class="mr-2">🎟️</span>
                  Acheter <%= subscription[:price] %> €
                </span>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Section Acheter des playlists -->
    <div class="mb-16">
      <div class="flex items-center mb-8">
        <h2 class="text-3xl font-bold text-white">Acheter des playlists</h2>
        <span class="ml-3 text-3xl">🎶</span>
      </div>
      
      <% if @premium_playlists.any? %>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <% @premium_playlists.each do |playlist| %>
            <div class="bg-gray-800 rounded-xl p-6 border border-gray-700 hover:border-green-500 transition-all duration-300 transform hover:scale-105">
              <div class="text-center">
                <h3 class="text-xl font-semibold text-white mb-2"><%= playlist.title %></h3>
                <p class="text-gray-400 text-sm mb-4"><%= truncate(playlist.description, length: 100) %></p>
                
                <div class="flex items-center justify-center mb-4">
                  <span class="text-2xl mr-2">💰</span>
                  <span class="text-green-400 font-bold text-lg"><%= playlist.points_required %> points</span>
                </div>
                
                <%= link_to buy_playlist_store_path(playlist), 
                          class: "w-full bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white font-semibold py-3 px-6 rounded-lg transition-all duration-300 transform hover:scale-105 inline-block text-center" do %>
                  <span class="flex items-center justify-center">
                    <span class="mr-2">🛒</span>
                    Acheter
                  </span>
                <% end %>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="text-center py-12">
          <div class="text-6xl mb-4">🎵</div>
          <p class="text-gray-400 text-lg">Aucune playlist premium disponible pour le moment.</p>
        </div>
      <% end %>
    </div>
  </div>
</div>

<style>
  /* Animations supplémentaires */
  .hover\:scale-105:hover {
    transform: scale(1.05);
  }
  
  .transform {
    transition: all 0.3s ease;
  }
  
  /* Effet de brillance sur les cartes */
  .bg-gradient-to-r {
    position: relative;
    overflow: hidden;
  }
  
  .bg-gradient-to-r::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
    transition: left 0.5s;
  }
  
  .bg-gradient-to-r:hover::before {
    left: 100%;
  }
</style> 