<div class="min-h-screen bg-gradient-to-br from-gray-900 via-purple-900 to-gray-900 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-2xl mx-auto">
    <div class="bg-gray-800 rounded-lg shadow-xl p-8 mb-6">
      <div class="text-center mb-8">
        <% if @payment_type == 'subscription' %>
          <h1 class="text-3xl font-bold text-white mb-2">💳 Paiement Abonnement VIP</h1>
        <% else %>
          <h1 class="text-3xl font-bold text-white mb-2">💳 Paiement Pack de Points</h1>
        <% end %>
        <p class="text-gray-400">Suivez les instructions ci-dessous pour compléter votre paiement</p>
      </div>

      <div class="bg-gradient-to-r from-purple-600/20 to-blue-600/20 border border-purple-500/30 rounded-lg p-6 mb-6">
        <div class="flex items-center justify-between mb-4">
          <span class="text-gray-300">Montant à payer :</span>
          <span class="text-2xl font-bold text-white"><%= number_to_currency(@amount, unit: "€", separator: ",", delimiter: " ") %></span>
        </div>
        <div class="flex items-center justify-between mb-2">
          <span class="text-gray-300">Type :</span>
          <span class="text-white font-semibold"><%= @description %></span>
        </div>
        <% if @points %>
          <div class="flex items-center justify-between">
            <span class="text-gray-300">Points :</span>
            <span class="text-yellow-400 font-semibold"><%= @points %> points</span>
          </div>
        <% end %>
        <% if @payment_type == 'points' && @pending_payment %>
          <div class="mt-4 pt-4 border-t border-gray-600">
            <p class="text-xs text-gray-400 text-center">
              💡 <strong>Important :</strong> Le montant doit correspondre exactement à l'élément sélectionné : <strong class="text-white"><%= number_to_currency(@amount, unit: "€", separator: ",", delimiter: " ") %></strong>
            </p>
          </div>
        <% end %>
      </div>

      <div class="space-y-6">
        <!-- Étape 1 -->
        <div class="bg-gray-700 rounded-lg p-6">
          <div class="flex items-start space-x-4">
            <div class="flex-shrink-0 w-8 h-8 bg-purple-600 rounded-full flex items-center justify-center text-white font-bold">
              1
            </div>
            <div class="flex-1">
              <h3 class="text-lg font-semibold text-white mb-2">Cliquez sur le lien PayPal.me</h3>
              <p class="text-gray-400 mb-4">Cliquez sur le bouton ci-dessous pour accéder à votre page de paiement PayPal</p>
              <a href="<%= @paypal_me_link %>" 
                 target="_blank" 
                 rel="noopener noreferrer"
                 class="inline-block bg-gradient-to-r from-blue-600 to-purple-600 text-white font-bold py-3 px-6 rounded-lg hover:from-blue-700 hover:to-purple-700 transition duration-200 text-center">
                💳 Payer avec PayPal.me
              </a>
              <p class="text-xs text-gray-500 mt-2">
                Lien : <span class="text-purple-400"><%= @paypal_me_link %></span>
              </p>
            </div>
          </div>
        </div>

        <!-- Étape 2 -->
        <div class="bg-gray-700 rounded-lg p-6">
          <div class="flex items-start space-x-4">
            <div class="flex-shrink-0 w-8 h-8 bg-purple-600 rounded-full flex items-center justify-center text-white font-bold">
              2
            </div>
            <div class="flex-1">
              <h3 class="text-lg font-semibold text-white mb-2">Entrez le montant</h3>
              <p class="text-gray-400 mb-2">
                <% if @payment_type == 'points' %>
                  Sur la page PayPal, entrez le montant suivant (doit correspondre à l'élément sélectionné dans ce cas <%= number_to_currency(@amount, unit: "€", separator: ",", delimiter: " ") %>) :
                <% else %>
                  Sur la page PayPal, entrez le montant suivant :
                <% end %>
              </p>
              <div class="bg-gray-800 rounded-lg p-4 mb-2">
                <p class="text-2xl font-bold text-center text-yellow-400">
                  <%= number_to_currency(@amount, unit: "€", separator: ",", delimiter: " ") %>
                </p>
              </div>
              <p class="text-xs text-gray-500">
                <% if @payment_type == 'points' %>
                  Assurez-vous d'entrer exactement ce montant > <%= number_to_currency(@amount, unit: "€", separator: ",", delimiter: " ") %>
                <% else %>
                  Assurez-vous d'entrer exactement ce montant
                <% end %>
              </p>
            </div>
          </div>
        </div>

        <!-- Étape 3 -->
        <div class="bg-gray-700 rounded-lg p-6">
          <div class="flex items-start space-x-4">
            <div class="flex-shrink-0 w-8 h-8 bg-purple-600 rounded-full flex items-center justify-center text-white font-bold">
              3
            </div>
            <div class="flex-1">
              <h3 class="text-lg font-semibold text-white mb-2">Complétez le paiement</h3>
              <p class="text-gray-400">Connectez-vous à PayPal et complétez le paiement</p>
            </div>
          </div>
        </div>

        <!-- Étape 4 -->
        <div class="bg-gray-700 rounded-lg p-6">
          <div class="flex items-start space-x-4">
            <div class="flex-shrink-0 w-8 h-8 bg-purple-600 rounded-full flex items-center justify-center text-white font-bold">
              4
            </div>
            <div class="flex-1">
              <h3 class="text-lg font-semibold text-white mb-2">Confirmez votre paiement</h3>
              <p class="text-gray-400 mb-4">Revenez sur cette page et confirmez votre paiement avec le numéro de transaction PayPal</p>
              <%= link_to "Confirmer mon paiement", store_payment_confirmation_path, 
                  class: "inline-block bg-gradient-to-r from-green-600 to-emerald-600 text-white font-bold py-2 px-4 rounded-lg hover:from-green-700 hover:to-emerald-700 transition duration-200" %>
            </div>
          </div>
        </div>
      </div>

      <div class="mt-8 bg-yellow-900/30 border border-yellow-700 rounded-lg p-4">
        <p class="text-yellow-200 text-sm">
          <strong>💡 Astuce :</strong> Après le paiement, PayPal vous enverra un email de confirmation. 
          Le numéro de transaction se trouve dans cet email (format : 5X1234567890123).
        </p>
      </div>

      <div class="mt-6 text-center">
        <%= link_to "← Retour à la boutique", store_path, class: "text-purple-400 hover:text-purple-300 underline" %>
      </div>
    </div>
  </div>
</div>

