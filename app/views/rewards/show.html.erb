<div class="min-h-screen bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900">
  <div class="container mx-auto px-4 py-8">
    <!-- Header avec logo -->
    <div class="text-center mb-8">
      <div class="flex items-center justify-center mb-6">
        <div class="w-16 h-16 bg-white/10 backdrop-blur-sm rounded-full flex items-center justify-center">
          <div class="w-8 h-8 text-purple-400">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
              <path d="M5 8h2v8H5zm7 0H9v8h3zm5 8h2V8h-2zM8 5h8v2H8z"/>
            </svg>
          </div>
        </div>
        <h1 class="text-4xl font-bold text-white ml-4">🎁 Détails de la Récompense</h1>
      </div>
      <p class="text-gray-300 text-lg">Découvrez le contenu de votre récompense</p>
    </div>

    <!-- Navigation -->
    <div class="flex justify-center mb-8">
      <div class="flex space-x-2">
        <%= link_to "← Retour aux récompenses", my_rewards_path, 
            class: "px-4 py-2 rounded-lg text-sm font-medium text-gray-300 bg-gray-700 hover:bg-gray-600 transition-colors" %>
      </div>
    </div>

    <!-- Détails de la récompense -->
    <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 mb-8">
      <div class="flex items-center justify-between mb-6">
        <div>
          <h2 class="text-2xl font-bold text-white mb-2">
            <%= @reward.content_type&.humanize || @reward.reward_type&.humanize %>
          </h2>
          <p class="text-gray-300"><%= @reward.reward_description %></p>
        </div>
        <div class="text-right">
          <div class="text-4xl">
            <% case @reward.reward_type %>
            <% when 'challenge' %>
              🥉
            <% when 'exclusif' %>
              🥈
            <% when 'premium' %>
              🥇
            <% when 'ultime' %>
              🌈
            <% end %>
          </div>
          <div class="text-sm text-gray-300">Débloqué le <%= @reward.unlocked_at&.strftime('%d/%m/%Y') %></div>
        </div>
      </div>

      <!-- Statistiques -->
      <div class="grid grid-cols-3 gap-4 mb-6">
        <div class="bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg p-4 text-center">
          <div class="text-2xl font-bold text-white"><%= @reward.quantity_required %></div>
          <div class="text-sm text-gray-200">Badges requis</div>
        </div>
        <div class="bg-gradient-to-r from-green-500 to-emerald-600 rounded-lg p-4 text-center">
          <div class="text-2xl font-bold text-white">
            <% if @reward.unlocked? %>
              ✅
            <% else %>
              🔒
            <% end %>
          </div>
          <div class="text-sm text-gray-200">
            <% if @reward.unlocked? %>
              Débloqué
            <% else %>
              Verrouillé
            <% end %>
          </div>
        </div>
        <div class="bg-gradient-to-r from-yellow-500 to-orange-600 rounded-lg p-4 text-center">
          <div class="text-2xl font-bold text-white">
            <% if @playlist %>
              <%= @playlist.videos.count %>
            <% else %>
              0
            <% end %>
          </div>
          <div class="text-sm text-gray-200">Vidéos</div>
        </div>
      </div>
    </div>

    <!-- Playlist associée -->
    <% if @playlist %>
      <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 mb-8">
        <h3 class="text-xl font-bold text-white mb-4">🎵 Playlist associée</h3>
        
        <div class="bg-gradient-to-r from-gray-700 to-gray-800 rounded-lg overflow-hidden border border-gray-600">
          <!-- Bannière de la playlist -->
          <div class="h-48 relative bg-gradient-to-r from-purple-500 to-pink-600 flex items-center justify-center">
            <div class="text-white text-6xl">🎵</div>
            <div class="absolute top-3 left-3">
              <span class="bg-green-500 text-white text-xs px-2 py-1 rounded-full">DÉBLOQUÉ</span>
            </div>
            <div class="absolute top-3 right-3">
              <span class="bg-white/20 text-white text-xs px-2 py-1 rounded-full"><%= @playlist.videos.count %> vidéos</span>
            </div>
          </div>
          
          <!-- Contenu de la playlist -->
          <div class="p-6">
            <h4 class="text-xl font-bold text-white mb-2"><%= @playlist.title %></h4>
            <p class="text-gray-300 mb-4"><%= @playlist.description %></p>
            
            <!-- Statistiques de la playlist -->
            <div class="grid grid-cols-3 gap-4 mb-6">
              <div class="text-center">
                <div class="text-2xl font-bold text-white"><%= @playlist.videos.count %></div>
                <div class="text-sm text-gray-300">Vidéos</div>
              </div>
              <div class="text-center">
                <div class="text-2xl font-bold text-white">
                  <% if @playlist.premium? %>
                    ⭐
                  <% else %>
                    🎵
                  <% end %>
                </div>
                <div class="text-sm text-gray-300">
                  <% if @playlist.premium? %>
                    Premium
                  <% else %>
                    Standard
                  <% end %>
                </div>
              </div>
              <div class="text-center">
                <div class="text-2xl font-bold text-white">
                  <% if @playlist.exclusive? %>
                    🔒
                  <% else %>
                    🌐
                  <% end %>
                </div>
                <div class="text-sm text-gray-300">
                  <% if @playlist.exclusive? %>
                    Exclusive
                  <% else %>
                    Public
                  <% end %>
                </div>
              </div>
            </div>
            
            <!-- Bouton pour accéder à la playlist -->
            <div class="text-center">
              <%= link_to "🎵 Écouter la playlist", playlist_path(@playlist), 
                  class: "bg-gradient-to-r from-purple-500 to-pink-600 text-white px-6 py-3 rounded-lg font-semibold hover:from-purple-600 hover:to-pink-700 transition duration-200" %>
            </div>
          </div>
        </div>
      </div>

      <!-- Liste des vidéos -->
      <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6">
        <h3 class="text-xl font-bold text-white mb-4">📺 Vidéos de la playlist</h3>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          <% @playlist.videos.each do |video| %>
            <div class="bg-gradient-to-r from-gray-700 to-gray-800 rounded-lg overflow-hidden border border-gray-600 hover:border-purple-500 transition-colors">
              <!-- Thumbnail YouTube -->
              <div class="relative h-32 bg-gray-800">
                <img src="https://img.youtube.com/vi/<%= video.youtube_id %>/mqdefault.jpg" 
                     alt="<%= video.title %>" 
                     class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-black bg-opacity-20 flex items-center justify-center">
                  <div class="text-white text-2xl">▶️</div>
                </div>
              </div>
              
              <!-- Informations de la vidéo -->
              <div class="p-4">
                <h4 class="text-white font-semibold text-sm mb-2 line-clamp-2"><%= video.title %></h4>
                <% if video.description.present? %>
                  <p class="text-gray-300 text-xs line-clamp-2"><%= video.description %></p>
                <% end %>
                
                <!-- Bouton pour voir la vidéo -->
                <div class="mt-3">
                  <a href="https://www.youtube.com/watch?v=<%= video.youtube_id %>" 
                     target="_blank" 
                     class="inline-flex items-center px-3 py-1 bg-red-600 text-white text-xs rounded hover:bg-red-700 transition-colors">
                    <span>Regarder</span>
                    <svg class="ml-1 w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M11 3a1 1 0 100 2h2.586l-6.293 6.293a1 1 0 101.414 1.414L15 6.414V9a1 1 0 102 0V4a1 1 0 00-1-1h-5z"/>
                    </svg>
                  </a>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    <% else %>
      <!-- Aucune playlist associée -->
      <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 text-center">
        <div class="text-6xl mb-4">🎁</div>
        <h3 class="text-xl font-bold text-white mb-2">Récompense surprise</h3>
        <p class="text-gray-300">Cette récompense ne contient pas de playlist associée.</p>
      </div>
    <% end %>
  </div>
</div>

<style>
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style> 