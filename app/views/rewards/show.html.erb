<div class="min-h-screen bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900">
  <div class="container mx-auto px-4 py-8">
    <!-- Header -->
    <div class="text-center mb-8">
      <h1 class="text-4xl font-bold text-white mb-4">🏆 Détails de la Récompense</h1>
      <p class="text-gray-300 text-lg">Informations détaillées sur cette récompense</p>
    </div>

    <!-- Bouton retour -->
    <div class="mb-8">
      <%= link_to "← Retour aux récompenses", my_rewards_path, 
          class: "text-blue-400 hover:text-blue-300 font-medium" %>
    </div>

    <!-- Détails de la récompense -->
    <div class="max-w-2xl mx-auto">
      <div class="bg-white/10 backdrop-blur-sm rounded-xl p-8 shadow-lg">
        <div class="flex items-center justify-between mb-6">
          <div class="text-4xl">
            <% case @reward.reward_type %>
            <% when 'challenge' %>
              🎯
            <% when 'exclusif' %>
              ⭐
            <% when 'premium' %>
              👑
            <% end %>
          </div>
          <div class="text-right">
            <% if @reward.unlocked? %>
              <span class="inline-block px-3 py-1 rounded-full text-sm font-medium bg-green-500 text-white">
                DÉBLOQUÉ
              </span>
            <% else %>
              <span class="inline-block px-3 py-1 rounded-full text-sm font-medium bg-gray-500 text-white">
                VERROUILLÉ
              </span>
            <% end %>
          </div>
        </div>

        <h2 class="text-2xl font-bold text-white mb-4">
          <%= @reward.reward_type.humanize %> - <%= @reward.badge_type.humanize %>
        </h2>

        <div class="space-y-4">
          <div>
            <h3 class="text-lg font-semibold text-white mb-2">Description</h3>
            <p class="text-gray-300"><%= @reward.reward_description %></p>
          </div>

          <div class="grid grid-cols-2 gap-4">
            <div>
              <h3 class="text-lg font-semibold text-white mb-2">Type de badge requis</h3>
              <p class="text-gray-300"><%= @reward.badge_type.humanize %></p>
            </div>
            <div>
              <h3 class="text-lg font-semibold text-white mb-2">Quantité requise</h3>
              <p class="text-gray-300"><%= @reward.quantity_required %> badges</p>
            </div>
          </div>

          <% if @reward.unlocked? %>
            <div>
              <h3 class="text-lg font-semibold text-white mb-2">Date de déblocage</h3>
              <p class="text-gray-300">
                <%= @reward.unlocked_at.strftime("%d/%m/%Y à %H:%M") %>
              </p>
            </div>
          <% end %>

          <!-- Progression actuelle -->
          <div>
            <h3 class="text-lg font-semibold text-white mb-2">Votre progression</h3>
            <% current_count = current_user.user_badges.joins(:badge).where(badges: { badge_type: @reward.badge_type }).count %>
            <% progress = [(current_count.to_f / @reward.quantity_required * 100), 100].min %>
            
            <div class="flex items-center justify-between mb-2">
              <span class="text-gray-300">
                <%= current_count %> / <%= @reward.quantity_required %> badges
              </span>
              <span class="text-gray-300">
                <%= progress.to_i %>%
              </span>
            </div>
            
            <div class="w-full bg-gray-600 rounded-full h-3">
              <div class="bg-gradient-to-r from-blue-500 to-purple-600 h-3 rounded-full transition-all duration-300" 
                   style="width: <%= progress %>%"></div>
            </div>
            
            <% if current_count >= @reward.quantity_required %>
              <p class="text-green-400 text-sm mt-2">✅ Vous avez assez de badges pour débloquer cette récompense !</p>
            <% else %>
              <p class="text-gray-400 text-sm mt-2">
                Il vous manque <%= @reward.quantity_required - current_count %> badge(s) pour débloquer cette récompense
              </p>
            <% end %>
          </div>
        </div>

        <!-- Actions -->
        <div class="mt-8 pt-6 border-t border-gray-600">
          <% if @reward.unlocked? %>
            <div class="text-center">
              <p class="text-green-400 font-medium">🎉 Cette récompense est débloquée !</p>
            </div>
          <% else %>
            <div class="text-center">
              <p class="text-gray-400 mb-4">Collectez plus de badges pour débloquer cette récompense</p>
              <%= link_to "Jouer pour gagner des badges", playlists_path, 
                  class: "inline-block bg-gradient-to-r from-blue-500 to-purple-600 hover:from-blue-600 hover:to-purple-700 text-white font-bold py-3 px-6 rounded-lg shadow-lg transform hover:scale-105 transition-all duration-200" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div> 