<div class="min-h-screen bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900">
  <div class="container mx-auto px-4 py-8">
    <!-- Header avec logo -->
    <div class="text-center mb-8">
      <div class="flex items-center justify-center mb-6">
        <div class="w-16 h-16 bg-white/10 backdrop-blur-sm rounded-full flex items-center justify-center">
          <div class="w-8 h-8 text-purple-400">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
              <path d="M5 8h2v8H5zm7 0H9v8h3zm5 8h2V8h-2zM8 5h8v2H8z"/>
            </svg>
          </div>
        </div>
        <h1 class="text-4xl font-bold text-white ml-4">🏆 Mes Récompenses</h1>
      </div>
      <p class="text-gray-300 text-lg">Débloquez des récompenses en combinant vos badges</p>
    </div>

    <!-- Navigation moderne -->
    <div class="flex justify-center mb-8">
      <div class="flex space-x-2">
        <%= link_to "Mes récompenses", my_rewards_path, 
            class: "px-4 py-2 rounded-lg text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 transition-colors" %>
        <%= link_to "Toutes les récompenses", all_rewards_path, 
            class: "px-4 py-2 rounded-lg text-sm font-medium text-gray-300 bg-gray-700 hover:bg-gray-600 transition-colors" %>
      </div>
    </div>

    <div class="container mx-auto px-4 py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900 mb-4">🎁 Mes Récompenses</h1>
    
    <!-- Progression vers la prochaine récompense -->
    <div class="bg-white rounded-lg shadow-md p-6 mb-6">
      <h2 class="text-xl font-semibold mb-4">📊 Progression</h2>
      <div class="flex items-center justify-between mb-4">
        <div class="text-sm text-gray-600">
          <span class="font-semibold"><%= @total_badges %></span> badges obtenus
        </div>
        <div class="text-sm text-gray-600">
          <% if @next_level && @next_level != 'completed' %>
            Prochaine récompense : <span class="font-semibold text-<%= @next_level == 'challenge' ? 'yellow' : @next_level == 'exclusif' ? 'gray' : @next_level == 'premium' ? 'orange' : 'pink' %>-600"><%= @next_level.humanize %></span>
          <% else %>
            <% 
              # Vérifier si toutes les récompenses sont réellement débloquées
              challenge_unlocked = @challenge_rewards.any?
              exclusif_unlocked = @exclusif_rewards.any?
              premium_unlocked = @premium_rewards.any?
              ultime_unlocked = @ultime_rewards.any?
              
              all_unlocked = challenge_unlocked && exclusif_unlocked && premium_unlocked && ultime_unlocked
            %>
            <% if all_unlocked %>
              Toutes les récompenses débloquées !
            <% else %>
              Continuez à collecter des badges !
            <% end %>
          <% end %>
        </div>
      </div>
      
      <% if @next_level %>
        <div class="w-full bg-gray-200 rounded-full h-2.5">
          <div class="bg-gradient-to-r from-blue-500 to-purple-600 h-2.5 rounded-full" style="width: <%= (@progress[0].to_f / @progress[1] * 100).round %>%"></div>
        </div>
        <div class="text-xs text-gray-500 mt-2">
          <%= @progress[0] %> / <%= @progress[1] %> badges
        </div>
      <% end %>
    </div>
  </div>

  <!-- Récompenses par niveau -->
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
    
    <!-- Challenge (3 badges) -->
    <div class="relative overflow-hidden rounded-xl shadow-lg border border-gray-700 h-64" style="background: linear-gradient(135deg, #f59e0b 0%, #d97706 50%, #b45309 100%);">
      <!-- Overlay sombre -->
      <div class="absolute inset-0 bg-black bg-opacity-20"></div>
      
      <!-- Contenu principal -->
      <div class="relative z-10 h-full flex flex-col justify-between p-6">
        <!-- En-tête -->
        <div class="flex items-center justify-between">
          <div class="text-4xl">🥉</div>
          <div class="text-right">
            <div class="text-2xl font-bold text-white">3</div>
            <div class="text-xs text-gray-300">badges</div>
          </div>
        </div>
        
        <!-- Contenu central -->
        <div class="text-center">
          <% if @challenge_rewards.any? %>
            <div class="text-6xl mb-2">🎁</div>
            <div class="text-white font-bold text-lg mb-1">DÉBLOQUÉ</div>
            <div class="text-gray-300 text-sm">Récompense surprise</div>
          <% else %>
            <div class="text-6xl mb-2">🔒</div>
            <div class="text-gray-400 font-bold text-lg mb-1">VERROUILLÉ</div>
            <div class="text-gray-500 text-sm">3 badges requis</div>
          <% end %>
        </div>
        
        <!-- Barre de progression -->
        <div class="w-full">
          <div class="flex justify-between text-xs text-gray-300 mb-1">
            <span>Progression</span>
            <span><%= @total_badges %>/3</span>
          </div>
          <div class="w-full bg-gray-700 rounded-full h-2">
            <div class="bg-gradient-to-r from-yellow-400 to-yellow-600 h-2 rounded-full transition-all duration-300" 
                 style="width: <%= [(@total_badges.to_f / 3 * 100).round, 100].min %>%"></div>
          </div>
        </div>
      </div>
    </div>

    <!-- Exclusif (6 badges) -->
    <div class="relative overflow-hidden rounded-xl shadow-lg border border-gray-700 h-64" style="background: linear-gradient(135deg, #6b7280 0%, #4b5563 50%, #374151 100%);">
      <!-- Overlay sombre -->
      <div class="absolute inset-0 bg-black bg-opacity-20"></div>
      
      <!-- Contenu principal -->
      <div class="relative z-10 h-full flex flex-col justify-between p-6">
        <!-- En-tête -->
        <div class="flex items-center justify-between">
          <div class="text-4xl">🥈</div>
          <div class="text-right">
            <div class="text-2xl font-bold text-white">6</div>
            <div class="text-xs text-gray-300">badges</div>
          </div>
        </div>
        
        <!-- Contenu central -->
        <div class="text-center">
          <% if @exclusif_rewards.any? %>
            <div class="text-6xl mb-2">🎁</div>
            <div class="text-white font-bold text-lg mb-1">DÉBLOQUÉ</div>
            <div class="text-gray-300 text-sm">Récompense surprise</div>
          <% else %>
            <div class="text-6xl mb-2">🔒</div>
            <div class="text-gray-400 font-bold text-lg mb-1">VERROUILLÉ</div>
            <div class="text-gray-500 text-sm">6 badges requis</div>
          <% end %>
        </div>
        
        <!-- Barre de progression -->
        <div class="w-full">
          <div class="flex justify-between text-xs text-gray-300 mb-1">
            <span>Progression</span>
            <span><%= @total_badges %>/6</span>
          </div>
          <div class="w-full bg-gray-700 rounded-full h-2">
            <div class="bg-gradient-to-r from-gray-400 to-gray-600 h-2 rounded-full transition-all duration-300" 
                 style="width: <%= [(@total_badges.to_f / 6 * 100).round, 100].min %>%"></div>
          </div>
        </div>
      </div>
    </div>

    <!-- Premium (9 badges) -->
    <div class="relative overflow-hidden rounded-xl shadow-lg border border-gray-700 h-64" style="background: linear-gradient(135deg, #ea580c 0%, #c2410c 50%, #9a3412 100%);">
      <!-- Overlay sombre -->
      <div class="absolute inset-0 bg-black bg-opacity-20"></div>
      
      <!-- Contenu principal -->
      <div class="relative z-10 h-full flex flex-col justify-between p-6">
        <!-- En-tête -->
        <div class="flex items-center justify-between">
          <div class="text-4xl">🥇</div>
          <div class="text-right">
            <div class="text-2xl font-bold text-white">9</div>
            <div class="text-xs text-gray-300">badges</div>
          </div>
        </div>
        
        <!-- Contenu central -->
        <div class="text-center">
          <% if @premium_rewards.any? %>
            <div class="text-6xl mb-2">🎁</div>
            <div class="text-white font-bold text-lg mb-1">DÉBLOQUÉ</div>
            <div class="text-gray-300 text-sm">Récompense surprise</div>
          <% else %>
            <div class="text-6xl mb-2">🔒</div>
            <div class="text-gray-400 font-bold text-lg mb-1">VERROUILLÉ</div>
            <div class="text-gray-500 text-sm">9 badges requis</div>
          <% end %>
        </div>
        
        <!-- Barre de progression -->
        <div class="w-full">
          <div class="flex justify-between text-xs text-gray-300 mb-1">
            <span>Progression</span>
            <span><%= @total_badges %>/9</span>
          </div>
          <div class="w-full bg-gray-700 rounded-full h-2">
            <div class="bg-gradient-to-r from-orange-400 to-orange-600 h-2 rounded-full transition-all duration-300" 
                 style="width: <%= [(@total_badges.to_f / 9 * 100).round, 100].min %>%"></div>
          </div>
        </div>
      </div>
    </div>

    <!-- Ultime (Collection Arc-en-ciel) -->
    <div class="relative overflow-hidden rounded-xl shadow-lg border border-gray-700 h-64" style="background: linear-gradient(135deg, #be185d 0%, #a21caf 50%, #7c3aed 100%);">
      <!-- Overlay sombre -->
      <div class="absolute inset-0 bg-black bg-opacity-20"></div>
      
      <!-- Contenu principal -->
      <div class="relative z-10 h-full flex flex-col justify-between p-6">
        <!-- En-tête -->
        <div class="flex items-center justify-between">
          <div class="text-4xl">🌈</div>
          <div class="text-right">
            <div class="text-lg font-bold text-white">Arc-en-ciel</div>
            <div class="text-xs text-gray-300">1B+1S+1G</div>
          </div>
        </div>
        
        <!-- Contenu central -->
        <div class="text-center">
          <% if @ultime_rewards.any? %>
            <div class="text-6xl mb-2">🎁</div>
            <div class="text-white font-bold text-lg mb-1">DÉBLOQUÉ</div>
            <div class="text-gray-300 text-sm">Récompense surprise</div>
          <% else %>
            <div class="text-6xl mb-2">🔒</div>
            <div class="text-gray-400 font-bold text-lg mb-1">VERROUILLÉ</div>
            <div class="text-gray-500 text-sm">Collection complète</div>
          <% end %>
        </div>
        
        <!-- Barre de progression -->
        <div class="w-full">
          <% rainbow_progress = current_user.has_rainbow_collection? ? 100 : 0 %>
          <div class="flex justify-between text-xs text-gray-300 mb-1">
            <span>Progression</span>
            <span><%= rainbow_progress %>%</span>
          </div>
          <div class="w-full bg-gray-700 rounded-full h-2">
            <div class="bg-gradient-to-r from-pink-400 to-purple-600 h-2 rounded-full transition-all duration-300" 
                 style="width: <%= rainbow_progress %>%"></div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Statistiques des badges -->
  <div class="mt-8 bg-black/80 backdrop-blur-sm rounded-xl shadow-lg p-6 border border-gray-700">
    <h2 class="text-xl font-semibold mb-4 text-white">📊 Statistiques des Badges</h2>
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
      <div class="text-center">
        <div class="text-2xl font-bold text-yellow-400"><%= @bronze_count %></div>
        <div class="text-sm text-gray-300">Bronze</div>
      </div>
      <div class="text-center">
        <div class="text-2xl font-bold text-gray-400"><%= @silver_count %></div>
        <div class="text-sm text-gray-300">Silver</div>
      </div>
      <div class="text-center">
        <div class="text-2xl font-bold text-yellow-300"><%= @gold_count %></div>
        <div class="text-sm text-gray-300">Gold</div>
      </div>
      <div class="text-center">
        <div class="text-2xl font-bold text-purple-400"><%= @total_badges %></div>
        <div class="text-sm text-gray-300">Total</div>
      </div>
    </div>
  </div>

  <!-- Bouton pour vérifier les nouvelles récompenses -->
  <div class="mt-6 text-center">
    <%= link_to "🔄 Vérifier les Nouvelles Récompenses", unlock_rewards_path, method: :post, class: "bg-gradient-to-r from-blue-500 to-purple-600 text-white px-6 py-3 rounded-lg font-semibold hover:from-blue-600 hover:to-purple-700 transition duration-200" %>
  </div>
</div>

<script>
// Animation pour les nouvelles récompenses
document.addEventListener('DOMContentLoaded', function() {
  const rewards = document.querySelectorAll('.bg-white.rounded-lg');
  
  rewards.forEach((reward, index) => {
    reward.style.animation = `fadeInUp 0.6s ease-out ${index * 0.1}s both`;
  });
});

// CSS pour l'animation
const style = document.createElement('style');
style.textContent = `
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
`;
document.head.appendChild(style);
</script> 