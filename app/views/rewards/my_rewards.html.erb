<div class="min-h-screen bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900">
  <div class="container mx-auto px-4 py-8">
    <!-- Header avec logo -->
    <div class="text-center mb-8">
      <div class="flex items-center justify-center mb-6">
        <div class="w-16 h-16 bg-white/10 backdrop-blur-sm rounded-full flex items-center justify-center">
          <div class="w-8 h-8 text-purple-400">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
              <path d="M5 8h2v8H5zm7 0H9v8h3zm5 8h2V8h-2zM8 5h8v2H8z"/>
            </svg>
          </div>
        </div>
        <h1 class="text-4xl font-bold text-white ml-4">🏆 Mes Récompenses</h1>
      </div>
      <p class="text-gray-300 text-lg">Débloquez des récompenses en combinant vos badges</p>
    </div>

    <!-- Navigation moderne -->
    <div class="flex justify-center mb-8">
      <div class="flex space-x-2">
        <%= link_to "Mes récompenses", my_rewards_path, 
            class: "px-4 py-2 rounded-lg text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 transition-colors" %>
        <%= link_to "Toutes les récompenses", all_rewards_path, 
            class: "px-4 py-2 rounded-lg text-sm font-medium text-gray-300 bg-gray-700 hover:bg-gray-600 transition-colors" %>
      </div>
    </div>

    <!-- Bouton pour vérifier les récompenses -->
    <div class="text-center mb-8">
      <%= link_to "Vérifier mes récompenses", unlock_rewards_path, method: :post, 
          class: "bg-gradient-to-r from-yellow-500 to-orange-500 hover:from-yellow-600 hover:to-orange-600 text-white font-bold py-3 px-6 rounded-lg shadow-lg transform hover:scale-105 transition-all duration-200" %>
    </div>

    <!-- Statistiques des badges -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
      <!-- Total badges -->
      <div class="bg-gradient-to-r from-blue-600 to-indigo-600 rounded-xl p-6 text-center">
        <div class="text-3xl font-bold text-white mb-2"><%= @total_badges %></div>
        <div class="text-gray-200">Total Badges</div>
      </div>
      
      <!-- Bronze -->
      <div class="bg-gradient-to-r from-amber-600 to-orange-600 rounded-xl p-6 text-center">
        <div class="text-3xl font-bold text-white mb-2"><%= @bronze_count %></div>
        <div class="text-gray-200">Bronze</div>
      </div>
      
      <!-- Silver -->
      <div class="bg-gradient-to-r from-gray-400 to-gray-600 rounded-xl p-6 text-center">
        <div class="text-3xl font-bold text-white mb-2"><%= @silver_count %></div>
        <div class="text-gray-200">Silver</div>
      </div>
      
      <!-- Gold -->
      <div class="bg-gradient-to-r from-yellow-500 to-yellow-600 rounded-xl p-6 text-center">
        <div class="text-3xl font-bold text-white mb-2"><%= @gold_count %></div>
        <div class="text-gray-200">Gold</div>
      </div>
    </div>

    <!-- Mes récompenses débloquées -->
    <div class="mb-12">
      <div class="bg-gradient-to-r from-green-600 to-emerald-600 rounded-xl p-6 mb-8">
        <h2 class="text-2xl font-bold text-white mb-6">🎉 Mes récompenses débloquées</h2>
        
        <% if @unlocked_rewards.any? %>
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% @unlocked_rewards.each do |reward| %>
              <div class="reward-card bg-white/10 backdrop-blur-sm rounded-xl overflow-hidden shadow-lg transform hover:scale-105 transition-all duration-200 h-full flex flex-col">
                <!-- Bannière avec image ou dégradé -->
                <div class="h-48 relative">
                  <div class="w-full h-full bg-gradient-to-r from-green-500 to-emerald-600 flex items-center justify-center">
                    <div class="text-white text-4xl">🏆</div>
                  </div>
                  <div class="absolute top-3 left-3">
                    <span class="bg-white/20 text-white text-xs px-2 py-1 rounded-full">NEW!</span>
                  </div>
                  <div class="absolute top-3 right-3">
                    <span class="bg-green-500 text-white text-xs px-2 py-1 rounded-full">DÉBLOQUÉ</span>
                  </div>
                </div>
                
                <!-- Contenu de la carte -->
                <div class="p-4 flex-1 flex flex-col">
                  <!-- Étiquettes -->
                  <div class="flex gap-2 mb-3">
                    <span class="bg-blue-500 text-white text-xs px-3 py-1 rounded-full">
                      <%= reward.reward_type.humanize %>
                    </span>
                    <span class="bg-white text-black text-xs px-3 py-1 rounded-full">
                      <%= reward.badge_type.humanize %>
                    </span>
                  </div>
                  
                  <!-- Titre -->
                  <h3 class="text-xl font-bold text-white mb-2">
                    <%= reward.reward_type.humanize %> <%= reward.badge_type.humanize %>
                  </h3>
                  
                  <!-- Description -->
                  <p class="text-gray-300 text-sm mb-4 flex-grow">
                    <%= reward.reward_description %>
                  </p>
                  
                  <!-- Détails en bas -->
                  <div class="flex items-center justify-between pt-3 border-t border-gray-600 mt-auto">
                    <span class="text-white text-sm"><%= reward.quantity_required %> badges</span>
                    <div class="text-center">
                      <div class="text-white font-bold"><%= reward.quantity_required * 100 %></div>
                      <div class="text-gray-400 text-xs">points</div>
                    </div>
                    <%= link_to reward_path(reward), class: "text-blue-400 hover:text-blue-300 text-sm flex items-center" do %>
                      Voir détails →
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="text-center py-12 bg-white/10 backdrop-blur-sm rounded-xl">
            <div class="text-6xl mb-4">🎯</div>
            <h3 class="text-xl font-bold text-white mb-2">Aucune récompense débloquée</h3>
            <p class="text-gray-400">Collectez des badges pour débloquer vos premières récompenses !</p>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Progression vers les récompenses -->
    <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 mt-8">
      <h2 class="text-2xl font-bold text-white mb-6">🎯 Progression vers les récompenses</h2>
      
      <!-- Récompenses par type de badge -->
      <div class="mb-8">
        <h3 class="text-xl font-bold text-white mb-4">📊 Par type de badge</h3>
        <% Badge.distinct.pluck(:badge_type).each do |badge_type| %>
          <% current_count = @badge_counts[badge_type] || 0 %>
          <div class="bg-gradient-to-r from-gray-700 to-gray-800 rounded-lg p-4 mb-4">
            <div class="flex items-center justify-between mb-3">
              <h4 class="text-lg font-bold text-white"><%= badge_type.humanize %></h4>
              <span class="text-xl font-bold text-blue-400"><%= current_count %> badges</span>
            </div>
            
            <!-- Progression pour ce type -->
            <div class="space-y-3">
              <% [3, 6, 9].each do |required| %>
                <% progress = [(current_count.to_f / required * 100), 100].min %>
                <div>
                  <div class="flex justify-between text-sm text-gray-300 mb-1">
                    <span><%= required %> badges <%= badge_type.humanize %></span>
                    <span><%= current_count %>/<%= required %></span>
                  </div>
                  <div class="w-full bg-gray-600 rounded-full h-2">
                    <div class="bg-gradient-to-r from-blue-500 to-purple-600 h-2 rounded-full transition-all duration-300" 
                         style="width: <%= progress %>%"></div>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
      
      <!-- Récompenses mixtes -->
      <div class="mb-8">
        <h3 class="text-xl font-bold text-white mb-4">🌈 Combinaisons mixtes</h3>
        <div class="bg-gradient-to-r from-gray-700 to-gray-800 rounded-lg p-4">
          <div class="flex items-center justify-between mb-3">
            <h4 class="text-lg font-bold text-white">Badges mixtes</h4>
            <span class="text-xl font-bold text-purple-400"><%= @total_badges %> badges</span>
          </div>
          
          <div class="space-y-3">
            <% [5, 8, 12].each do |required| %>
              <% progress = [(@total_badges.to_f / required * 100), 100].min %>
              <div>
                <div class="flex justify-between text-sm text-gray-300 mb-1">
                  <span><%= required %> badges mixtes</span>
                  <span><%= @total_badges %>/<%= required %></span>
                </div>
                <div class="w-full bg-gray-600 rounded-full h-2">
                  <div class="bg-gradient-to-r from-purple-500 to-pink-600 h-2 rounded-full transition-all duration-300" 
                       style="width: <%= progress %>%"></div>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
      
      <!-- Récompenses par niveau -->
      <div class="mb-8">
        <h3 class="text-xl font-bold text-white mb-4">🏅 Par niveau de badge</h3>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
          <!-- Bronze -->
          <div class="bg-gradient-to-r from-amber-600 to-orange-600 rounded-lg p-4">
            <h4 class="text-lg font-bold text-white mb-2">Bronze</h4>
            <div class="text-2xl font-bold text-white mb-2"><%= @bronze_count %></div>
            <div class="text-sm text-gray-200">
              <% if @bronze_count >= 3 %>
                ✅ Challenge débloqué
              <% else %>
                <%= 3 - @bronze_count %> de plus pour Challenge
              <% end %>
            </div>
          </div>
          
          <!-- Silver -->
          <div class="bg-gradient-to-r from-gray-400 to-gray-600 rounded-lg p-4">
            <h4 class="text-lg font-bold text-white mb-2">Silver</h4>
            <div class="text-2xl font-bold text-white mb-2"><%= @silver_count %></div>
            <div class="text-sm text-gray-200">
              <% if @silver_count >= 2 %>
                ✅ Exclusif débloqué
              <% else %>
                <%= 2 - @silver_count %> de plus pour Exclusif
              <% end %>
            </div>
          </div>
          
          <!-- Gold -->
          <div class="bg-gradient-to-r from-yellow-500 to-yellow-600 rounded-lg p-4">
            <h4 class="text-lg font-bold text-white mb-2">Gold</h4>
            <div class="text-2xl font-bold text-white mb-2"><%= @gold_count %></div>
            <div class="text-sm text-gray-200">
              <% if @gold_count >= 1 %>
                ✅ Premium débloqué
              <% else %>
                1 de plus pour Premium
              <% end %>
            </div>
          </div>
        </div>
        
        <!-- Rainbow collection -->
        <% if @bronze_count >= 1 && @silver_count >= 1 && @gold_count >= 1 %>
          <div class="mt-4 bg-gradient-to-r from-red-500 via-yellow-500 to-purple-500 rounded-lg p-4 text-center">
            <div class="text-2xl mb-2">🌈</div>
            <h4 class="text-lg font-bold text-white mb-2">Collection Arc-en-ciel</h4>
            <p class="text-white text-sm">Bronze + Silver + Gold = Récompense ultime !</p>
          </div>
        <% end %>
      </div>
    </div>
    
    <!-- Guide des récompenses -->
    <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 mt-8">
      <h2 class="text-2xl font-bold text-white mb-6">📖 Guide des récompenses</h2>
      
      <!-- Niveaux de récompenses -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <div class="bg-gradient-to-r from-green-500 to-emerald-600 rounded-lg p-6 text-center">
          <div class="text-4xl mb-4">🎯</div>
          <h3 class="text-xl font-bold text-white mb-2">Challenge (3 badges)</h3>
          <p class="text-green-100 text-sm">
            Collectez 3 badges du même type pour débloquer des défis spéciaux
          </p>
          <div class="mt-4 text-xs text-green-200">
            <p>• Accès anticipé à des playlists</p>
            <p>• Contenu exclusif podcast</p>
            <p>• Coffres surprises mensuels</p>
          </div>
        </div>

        <div class="bg-gradient-to-r from-blue-500 to-indigo-600 rounded-lg p-6 text-center">
          <div class="text-4xl mb-4">⭐</div>
          <h3 class="text-xl font-bold text-white mb-2">Exclusif (6 badges)</h3>
          <p class="text-blue-100 text-sm">
            Collectez 6 badges du même type pour accéder à du contenu exclusif
          </p>
          <div class="mt-4 text-xs text-blue-200">
            <p>• Photos dédicacées</p>
            <p>• Messages vidéo d'artistes</p>
            <p>• Trophées digitaux animés</p>
          </div>
        </div>

        <div class="bg-gradient-to-r from-purple-500 to-pink-600 rounded-lg p-6 text-center">
          <div class="text-4xl mb-4">👑</div>
          <h3 class="text-xl font-bold text-white mb-2">Premium (9 badges)</h3>
          <p class="text-purple-100 text-sm">
            Collectez 9 badges du même type pour débloquer des récompenses premium
          </p>
          <div class="mt-4 text-xs text-purple-200">
            <p>• Invitations concerts virtuels</p>
            <p>• Accès backstage virtuel</p>
            <p>• Invitations VIP concerts</p>
          </div>
        </div>
      </div>

      <!-- Types de badges -->
      <div>
        <h3 class="text-xl font-bold text-white mb-4">Types de badges disponibles :</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div class="bg-gradient-to-r from-amber-500 to-orange-600 rounded-lg p-4">
            <h4 class="text-lg font-bold text-white mb-2">Competitor</h4>
            <p class="text-amber-100 text-sm">Badges basés sur les points gagnés et les performances compétitives</p>
            <div class="mt-2 text-xs text-amber-200">
              <p>• Coffres surprises mensuels</p>
              <p>• Trophées digitaux animés</p>
              <p>• Invitations VIP concerts</p>
            </div>
          </div>
          
          <div class="bg-gradient-to-r from-blue-500 to-cyan-600 rounded-lg p-4">
            <h4 class="text-lg font-bold text-white mb-2">Engager</h4>
            <p class="text-blue-100 text-sm">Badges basés sur l'engagement (likes/dislikes) et la participation communautaire</p>
            <div class="mt-2 text-xs text-blue-200">
              <p>• Accès anticipé à des playlists</p>
              <p>• Photos dédicacées</p>
              <p>• Invitations concerts virtuels</p>
            </div>
          </div>
          
          <div class="bg-gradient-to-r from-red-500 to-pink-600 rounded-lg p-4">
            <h4 class="text-lg font-bold text-white mb-2">Critic</h4>
            <p class="text-red-100 text-sm">Badges basés sur les critiques et l'évaluation des contenus</p>
            <div class="mt-2 text-xs text-red-200">
              <p>• Accès à du contenu exclusif podcast</p>
              <p>• Messages vidéo d'artistes</p>
              <p>• Accès backstage virtuel</p>
            </div>
          </div>
          
          <div class="bg-gradient-to-r from-purple-500 to-violet-600 rounded-lg p-4">
            <h4 class="text-lg font-bold text-white mb-2">Challenger</h4>
            <p class="text-purple-100 text-sm">Badges basés sur la performance globale et les défis accomplis</p>
            <div class="mt-2 text-xs text-purple-200">
              <p>• Invitations bêtas de contenus exclusifs</p>
              <p>• Merch exclusif</p>
              <p>• Accès backstage réel</p>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Collection Arc-en-ciel -->
      <div class="mt-8 bg-gradient-to-r from-red-500 via-yellow-500 to-purple-500 rounded-lg p-6 text-center">
        <div class="text-4xl mb-4">🌈</div>
        <h3 class="text-xl font-bold text-white mb-2">Collection Arc-en-ciel</h3>
        <p class="text-white text-sm mb-4">
          Collectez 1 Bronze + 1 Silver + 1 Gold pour débloquer la récompense ultime !
        </p>
        <div class="text-xs text-white/80">
          <p>• Accès backstage réel</p>
          <p>• Rencontre avec un artiste</p>
          <p>• Merch exclusif premium</p>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .reward-card {
    border: 2px solid transparent;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
  }
  
  .reward-card:hover {
    border-color: #8b5cf6;
    box-shadow: 0 20px 25px -5px rgba(139, 92, 246, 0.1), 0 10px 10px -5px rgba(139, 92, 246, 0.04);
    transform: translateY(-5px);
  }
  
  @keyframes gradient {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }
</style> 