<div class="min-h-screen bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900">
  <div class="container mx-auto px-4 py-8">
    <!-- Header avec navigation -->
    <div class="text-center mb-8">
      <h1 class="text-4xl font-bold text-white mb-4">🏆 Toutes les Récompenses</h1>
      <p class="text-gray-300 text-lg">Découvrez toutes les récompenses disponibles sur la plateforme</p>
      
      <!-- Navigation entre les pages de récompenses -->
      <div class="flex justify-center mt-6">
        <div class="bg-white/10 backdrop-blur-sm rounded-lg p-1">
          <button onclick="window.location.href='<%= my_rewards_path %>'"
                  class="px-6 py-3 text-sm font-medium rounded-l-lg text-white bg-gray-700 hover:bg-gray-600 focus:outline-none">
            Mes récompenses
          </button>
          <button onclick="window.location.href='<%= all_rewards_path %>'"
                  class="px-6 py-3 text-sm font-medium rounded-r-lg text-white bg-blue-600 hover:bg-blue-700 focus:outline-none">
            Toutes les récompenses
          </button>
        </div>
      </div>
    </div>

    <!-- Statistiques globales -->
    <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 mb-8">
      <h2 class="text-2xl font-bold text-white mb-4">📊 Statistiques globales</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div class="bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg p-4 text-center">
          <div class="text-2xl font-bold text-white"><%= @total_rewards %></div>
          <div class="text-sm text-gray-200">Total récompenses</div>
        </div>
        <div class="bg-gradient-to-r from-green-500 to-emerald-600 rounded-lg p-4 text-center">
          <div class="text-2xl font-bold text-white"><%= @unlocked_rewards %></div>
          <div class="text-sm text-gray-200">Récompenses débloquées</div>
        </div>
        <div class="bg-gradient-to-r from-gray-500 to-gray-600 rounded-lg p-4 text-center">
          <div class="text-2xl font-bold text-white"><%= @locked_rewards %></div>
          <div class="text-sm text-gray-200">Récompenses verrouillées</div>
        </div>
      </div>
    </div>

    <!-- Toutes les récompenses par type de badge -->
    <% @rewards_by_type.each do |badge_type, rewards| %>
      <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 mb-8">
        <h3 class="text-2xl font-bold text-white mb-6">
          🎯 Récompenses <%= badge_type.humanize %>
        </h3>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <% [3, 5, 10].each do |quantity| %>
            <% reward = rewards.find { |r| r.quantity_required == quantity } %>
            
            <div class="bg-gradient-to-r from-gray-700 to-gray-800 rounded-xl p-6 border border-gray-600">
              <div class="flex items-center justify-between mb-4">
                <div class="text-2xl">🏆</div>
                <span class="text-gray-400 text-sm font-medium">
                  <%= quantity %> badges requis
                </span>
              </div>
              
              <h4 class="text-lg font-bold text-white mb-2">
                <%= case quantity
                    when 3 then 'Challenge'
                    when 5 then 'Exclusif'
                    when 10 then 'Premium'
                    end %>
              </h4>
              
              <p class="text-gray-300 text-sm mb-4">
                <%= case quantity
                    when 3 then 'Défi spécial débloqué'
                    when 5 then 'Contenu exclusif débloqué'
                    when 10 then 'Récompense premium débloquée'
                    end %>
              </p>
              
              <div class="text-xs text-gray-400">
                Collectez <%= quantity %> badges <%= badge_type.humanize %> pour débloquer cette récompense
              </div>
              
              <!-- Statistiques d'utilisation -->
              <% if reward %>
                <div class="mt-4 pt-4 border-t border-gray-600">
                  <div class="text-xs text-gray-400">
                    <% unlocked_count = reward.class.where(badge_type: badge_type, quantity_required: quantity, unlocked: true).count %>
                    <%= unlocked_count %> utilisateur(s) ont débloqué cette récompense
                  </div>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>

    <!-- Comment débloquer des récompenses -->
    <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6">
      <h2 class="text-2xl font-bold text-white mb-4">💡 Comment débloquer des récompenses ?</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="text-center">
          <div class="text-4xl mb-4">🎯</div>
          <h3 class="text-lg font-bold text-white mb-2">Collectez des badges</h3>
          <p class="text-gray-300 text-sm">Jouez et gagnez des badges en participant aux compétitions</p>
        </div>
        
        <div class="text-center">
          <div class="text-4xl mb-4">📊</div>
          <h3 class="text-lg font-bold text-white mb-2">Atteignez les seuils</h3>
          <p class="text-gray-300 text-sm">Collectez 3, 5 ou 10 badges du même type pour débloquer des récompenses</p>
        </div>
        
        <div class="text-center">
          <div class="text-4xl mb-4">🏆</div>
          <h3 class="text-lg font-bold text-white mb-2">Profitez des récompenses</h3>
          <p class="text-gray-300 text-sm">Débloquez des contenus exclusifs et des fonctionnalités premium</p>
        </div>
      </div>
    </div>
  </div>
</div> 