<div class="min-h-screen bg-gray-900 py-0">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- En-tête avec logo -->
    <div class="text-center mb-4">
      <h1 class="text-2xl font-bold text-white">🔍 <%= t('rewards.explore_rewards') %></h1>
    </div>

    <!-- Navigation moderne -->
    <div class="flex justify-center mb-8">
      <div class="flex space-x-2">
        <%= link_to t('rewards.my_rewards'), my_rewards_path, 
            class: "px-4 py-2 rounded-lg text-sm font-medium text-gray-300 bg-gray-700 hover:bg-gray-600 transition-colors" %>
        <%= link_to t('rewards.all_rewards'), all_rewards_path, 
            class: "px-4 py-2 rounded-lg text-sm font-medium text-white bg-purple-600 hover:bg-purple-700 transition-colors" %>
      </div>
    </div>



    <!-- Toutes les récompenses disponibles -->
    <div class="mb-8">
      <h2 class="text-2xl font-bold text-white mb-6">🎁 <%= t('rewards.all_available_rewards') %></h2>
      
      <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 mb-6">
        <h3 class="text-xl font-bold text-white mb-4">
          <%= t('rewards.all_available_rewards') %>
        </h3>
        
        <div class="rewards-grid">
          <% 
            reward_types = [
              { 
                quantity: 3, 
                level: 'challenge', 
                color: 'from-yellow-400 to-yellow-600', 
                icon: '🥉',
                content: 'Playlists Spéciales, Versions acoustiques, Remix spéciaux',
                visual: 'https://img.youtube.com/vi/qB7kLilZWwg/maxresdefault.jpg'
              },
              { 
                quantity: 6, 
                level: 'exclusif', 
                color: 'from-gray-400 to-gray-600', 
                icon: '🥈',
                content: 'Contenu exclusif des blogs et médias spécialisés, podcasts, documentaires, reportages, commentaires audio, vidéo session studio',
                visual: 'https://img.youtube.com/vi/9ECNWJ1R0fg/maxresdefault.jpg'
              },
              { 
                quantity: 9, 
                level: 'premium', 
                color: 'from-orange-400 to-orange-600', 
                icon: '🥇',
                content: 'Rencontres artistes, Backstage virtuel, Sessions privées, Contenu premium exclusif',
                visual: get_premium_preview_image
              },
              { 
                quantity: 12, 
                level: 'ultime', 
                color: 'from-pink-400 to-purple-600', 
                icon: '🌈',
                content: 'Rencontre privée, Backstage réel, Expériences VIP, Accès exclusif total',
                visual: get_ultime_preview_image
              }
            ]
          %>
          
          <% reward_types.each do |reward_info| %>
            <% current_count = current_user.user_badges.count %>
            <% progress = [(current_count.to_f / reward_info[:quantity] * 100), 100].min %>
            <% has_reward = current_user.has_reward_for_level?(reward_info[:level]) %>
            
            <% 
              # Créer un hash temporaire pour le helper
              temp_reward = {
                reward_type: reward_info[:level],
                unlocked: has_reward,
                quantity_required: reward_info[:quantity],
                reward_description: reward_info[:content]
              }
            %>
            <%= unified_reward_card(temp_reward) %>
          <% end %>
        </div>
      </div>
    </div>

    <!-- Guide du système -->
    <div class="bg-gradient-to-r from-blue-500/20 to-purple-600/20 backdrop-blur-sm rounded-xl p-6 border border-blue-400/30">
      <h2 class="text-2xl font-bold text-white mb-4">📖 <%= t('rewards.guide_title') %></h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <h4 class="text-lg font-bold text-white mb-3">🎯 <%= t('rewards.how_it_works_title') %></h4>
          <p class="text-gray-300 text-sm mb-3"><%= t('rewards.how_it_works_description') %></p>
          <div class="space-y-2 text-sm text-gray-300">
            <p>• <strong><%= t('rewards.badges_count', count: 3) %></strong> = 🥉 <%= t('rewards.levels.challenge') %> (<%= t('rewards.levels.challenge_content') %>)</p>
            <p>• <strong><%= t('rewards.badges_count', count: 6) %></strong> = 🥈 <%= t('rewards.levels.exclusif') %> (<%= t('rewards.levels.exclusif_content') %>)</p>
            <p>• <strong><%= t('rewards.badges_count', count: 9) %></strong> = 🥇 <%= t('rewards.levels.premium') %> (<%= t('rewards.levels.premium_content') %>)</p>
            <p>• <strong><%= t('rewards.badges_count', count: 12) %></strong> = 🌈 <%= t('rewards.levels.ultime') %> (<%= t('rewards.levels.ultime_content') %>)</p>
          </div>
        </div>

        <div>
          <h4 class="text-lg font-bold text-white mb-3">🚀 <%= t('rewards.strategies_title') %></h4>
          <p class="text-gray-300 text-sm mb-3"><%= t('rewards.strategies_description') %></p>
          <div class="space-y-2 text-sm text-gray-300">
            <p>• <strong><%= t('rewards.strategies.play_regularly') %></strong> <%= t('rewards.strategies.play_regularly_desc') %></p>
            <p>• <strong><%= t('rewards.strategies.engage_actively') %></strong> <%= t('rewards.strategies.engage_actively_desc') %></p>
            <p>• <strong><%= t('rewards.strategies.give_reviews') %></strong> <%= t('rewards.strategies.give_reviews_desc') %></p>
          </div>
        </div>
      </div>
      
      
      <div class="mt-6 p-4 bg-white/5 rounded-lg">
        <h3 class="text-lg font-bold text-white mb-2">💡 <%= t('rewards.expert_tips_title') %>:</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
          <div>
            <span class="text-yellow-400 font-semibold">🎵 <%= t('rewards.tips.diversify') %>:</span>
            <span class="text-gray-300"><%= t('rewards.tips.diversify_desc') %></span>
          </div>
          <div>
            <span class="text-purple-400 font-semibold">⏰ <%= t('rewards.tips.regularity') %>:</span>
            <span class="text-gray-300"><%= t('rewards.tips.regularity_desc') %></span>
          </div>
          <div>
            <span class="text-blue-400 font-semibold">🎯 <%= t('rewards.tips.goals') %>:</span>
            <span class="text-gray-300"><%= t('rewards.tips.goals_desc') %></span>
          </div>
          <div>
            <span class="text-purple-400 font-semibold">🏆 <%= t('rewards.tips.excellence') %>:</span>
            <span class="text-gray-300"><%= t('rewards.tips.excellence_desc') %></span>
          </div>
        </div>
      </div>
    </div>
<style>
  /* Harmonisation des titres principaux */
  h1.text-2xl.font-bold.text-white {
    font-size: 1.5rem !important;
    font-weight: 700 !important;
    line-height: 1.2 !important;
  }
</style>

<style>
  .reward-card {
    border: 2px solid transparent;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
  }
  
  .reward-card:hover {
    border-color: #8b5cf6;
    box-shadow: 0 20px 25px -5px rgba(139, 92, 246, 0.1), 0 10px 10px -5px rgba(139, 92, 246, 0.04);
    transform: translateY(-5px);
  }
  
  @keyframes gradient {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }
</style> 