<div class="min-h-screen bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900">
  <div class="container mx-auto px-4 py-8">
    <!-- Header avec logo -->
    <div class="text-center mb-8">
      <div class="flex items-center justify-center mb-6">
        <div class="w-16 h-16 bg-white/10 backdrop-blur-sm rounded-full flex items-center justify-center">
          <div class="w-8 h-8 text-purple-400">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
              <path d="M5 8h2v8H5zm7 0H9v8h3zm5 8h2V8h-2zM8 5h8v2H8z"/>
            </svg>
          </div>
        </div>
        <h1 class="text-4xl font-bold text-white ml-4">🎯 Objectifs et Défis</h1>
      </div>
      <p class="text-gray-300 text-lg">Découvrez tout ce que vous pouvez accomplir et vos objectifs à atteindre</p>
    </div>

    <!-- Navigation moderne -->
    <div class="flex justify-center mb-8">
      <div class="flex space-x-2">
        <%= link_to "Mes récompenses", my_rewards_path, 
            class: "px-4 py-2 rounded-lg text-sm font-medium text-gray-300 bg-gray-700 hover:bg-gray-600 transition-colors" %>
        <%= link_to "Toutes les récompenses", all_rewards_path, 
            class: "px-4 py-2 rounded-lg text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 transition-colors" %>
      </div>
    </div>

    <!-- Mes accomplissements -->
    <div class="bg-gradient-to-r from-green-500/20 to-emerald-600/20 backdrop-blur-sm rounded-xl p-6 mb-8 border border-green-400/30">
      <h2 class="text-2xl font-bold text-white mb-4">🎉 Mes Accomplissements</h2>
      <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
        <div class="bg-gradient-to-r from-green-500 to-emerald-600 rounded-lg p-4 text-center">
          <div class="text-3xl font-bold text-white"><%= current_user.user_badges.count %></div>
          <div class="text-sm text-gray-200">Badges collectés</div>
        </div>
        <div class="bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg p-4 text-center">
          <div class="text-3xl font-bold text-white"><%= current_user.rewards.where(unlocked: true).count %></div>
          <div class="text-sm text-gray-200">Récompenses débloquées</div>
        </div>
        <div class="bg-gradient-to-r from-yellow-500 to-orange-600 rounded-lg p-4 text-center">
          <div class="text-3xl font-bold text-white"><%= current_user.user_badges.joins(:badge).where(badges: { level: 'gold' }).count %></div>
          <div class="text-sm text-gray-200">Badges Gold</div>
        </div>
        <div class="bg-gradient-to-r from-purple-500 to-pink-600 rounded-lg p-4 text-center">
          <div class="text-3xl font-bold text-white"><%= (current_user.user_badges.count.to_f / 12 * 100).round %>%</div>
          <div class="text-sm text-gray-200">Progression globale</div>
        </div>
      </div>
    </div>

    <!-- Objectifs à atteindre -->
    <div class="mb-8">
      <h2 class="text-2xl font-bold text-white mb-6">🎯 Objectifs à Atteindre</h2>
      
      <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 mb-6">
        <h3 class="text-xl font-bold text-white mb-4">
          Progression vers les Récompenses
          <span class="text-sm font-normal text-gray-300">
            (<%= current_user.user_badges.count %> badges collectés sur 12 requis)
          </span>
        </h3>
        
        <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
          <% [
            { quantity: 3, level: 'challenge', color: 'from-yellow-400 to-yellow-600', icon: '🥉' },
            { quantity: 6, level: 'exclusif', color: 'from-gray-400 to-gray-600', icon: '🥈' },
            { quantity: 9, level: 'premium', color: 'from-orange-400 to-orange-600', icon: '🥇' },
            { quantity: 12, level: 'ultime', color: 'from-pink-400 to-purple-600', icon: '🌈' }
          ].each do |reward_info| %>
            <% current_count = current_user.user_badges.count %>
            <% progress = [(current_count.to_f / reward_info[:quantity] * 100), 100].min %>
            <% has_reward = current_user.has_reward_for_level?(reward_info[:level]) %>
            
            <div class="reward-card bg-gradient-to-r from-gray-700 to-gray-800 rounded-lg overflow-hidden border border-gray-600 transform hover:scale-105 transition-all duration-200">
              <!-- Bannière avec image -->
              <div class="h-48 relative">
                <div class="w-full h-full bg-gradient-to-r <%= reward_info[:color] %> flex items-center justify-center">
                  <div class="text-white text-4xl">🎁</div>
                </div>
                <div class="absolute top-3 left-3">
                  <% if has_reward %>
                    <span class="bg-green-500 text-white text-xs px-2 py-1 rounded-full">DÉBLOQUÉ</span>
                  <% else %>
                    <span class="bg-white/20 text-white text-xs px-2 py-1 rounded-full">SURPRISE</span>
                  <% end %>
                </div>
              </div>
              
              <!-- Contenu de la carte -->
              <div class="p-4">
                <!-- En-tête avec quantité -->
                <div class="text-center mb-4">
                  <div class="text-3xl font-bold text-white mb-2"><%= reward_info[:quantity] %></div>
                  <div class="text-sm text-gray-300">badges requis</div>
                </div>
                
                <!-- Type de récompense -->
                <div class="text-center mb-4">
                  <div class="text-lg font-semibold text-white mb-1">
                    <% if has_reward || current_count >= reward_info[:quantity] %>
                      <%= reward_info[:icon] %> <%= reward_info[:level].humanize %>
                    <% else %>
                      🎭 ???
                    <% end %>
                  </div>
                  <div class="text-xs text-gray-400">
                    <% if has_reward || current_count >= reward_info[:quantity] %>
                      <%= case reward_info[:quantity]
                          when 3 then 'Récompense Surprise'
                          when 6 then 'Récompense Surprise'
                          when 9 then 'Récompense Surprise'
                          when 12 then 'Récompense Surprise'
                          end %>
                    <% else %>
                      Mystère à découvrir...
                    <% end %>
                  </div>
                </div>
                
                <!-- Progression -->
                <div class="mb-4">
                  <% if has_reward || current_count >= reward_info[:quantity] %>
                    <div class="flex justify-between text-xs text-gray-400 mb-2">
                      <span>Progression</span>
                      <span><%= current_count %>/<%= reward_info[:quantity] %></span>
                    </div>
                    <div class="w-full bg-gray-600 rounded-full h-2">
                      <div class="bg-gradient-to-r from-blue-500 to-purple-600 h-2 rounded-full transition-all duration-300" 
                           style="width: <%= progress %>%"></div>
                    </div>
                  <% else %>
                    <div class="text-center py-3">
                      <div class="text-gray-500 text-sm">🔒 Récompense mystère</div>
                      <div class="text-gray-400 text-xs mt-1">Collectez plus de badges pour révéler</div>
                    </div>
                  <% end %>
                </div>
                
                <!-- Statut et lien détails -->
                <div class="flex items-center justify-between">
                  <div class="text-center">
                    <% if current_count >= reward_info[:quantity] %>
                      <div class="text-green-400 text-sm font-medium">
                        ✅ Prêt à débloquer !
                      </div>
                    <% else %>
                      <div class="text-gray-500 text-sm">
                        🔒 Il manque <%= reward_info[:quantity] - current_count %> badge(s)
                      </div>
                    <% end %>
                  </div>
                  <% 
                    # Utiliser des routes spécifiques selon le type de récompense
                    details_path = case reward_info[:quantity]
                    when 3 then reward_details_path(badge_type: 'unified', quantity: 3)
                    when 6 then reward_details_path(badge_type: 'unified', quantity: 6)
                    when 9 then reward_details_path(badge_type: 'unified', quantity: 9)
                    when 12 then reward_details_path(badge_type: 'unified', quantity: 12)
                    else reward_details_path(badge_type: 'unified', quantity: reward_info[:quantity])
                    end
                  %>
                  <% if has_reward || current_count >= reward_info[:quantity] %>
                    <%= link_to details_path, 
                        class: "text-blue-400 hover:text-blue-300 text-sm flex items-center" do %>
                      Détails →
                    <% end %>
                  <% else %>
                    <span class="text-gray-500 text-sm">🔒 Mystère</span>
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <!-- Guide du système -->
    <div class="bg-gradient-to-r from-blue-500/20 to-purple-600/20 backdrop-blur-sm rounded-xl p-6 border border-blue-400/30">
      <h2 class="text-2xl font-bold text-white mb-4">📖 Guide du Système de Récompenses</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <h4 class="text-lg font-bold text-white mb-3">🎯 Comment ça marche ?</h4>
          <p class="text-gray-300 text-sm mb-3">Le système est basé sur le total de badges collectés</p>
          <div class="space-y-2 text-sm text-gray-300">
            <p>• <strong>3 badges</strong> = 🥉 Challenge (Accès anticipé + Codes promo)</p>
            <p>• <strong>6 badges</strong> = 🥈 Exclusif (Photos dédicacées + Contenu exclusif)</p>
            <p>• <strong>9 badges</strong> = 🥇 Premium (Rencontres artistes + Backstage virtuel)</p>
            <p>• <strong>12 badges</strong> = 🌈 Ultime (Rencontre privée + Backstage réel)</p>
          </div>
        </div>

        <div>
          <h4 class="text-lg font-bold text-white mb-3">🚀 Stratégies pour Progresser</h4>
          <p class="text-gray-300 text-sm mb-3">Maximisez vos chances de débloquer des récompenses</p>
          <div class="space-y-2 text-sm text-gray-300">
            <p>• <strong>Jouez régulièrement</strong> aux playlists pour gagner des badges</p>
            <p>• <strong>Engagez-vous activement</strong> avec les contenus (likes/dislikes)</p>
            <p>• <strong>Donnez des critiques détaillées</strong> sur les musiques</p>
            <p>• <strong>Participez aux défis</strong> pour des badges bonus</p>
          </div>
        </div>
      </div>
      
      <div class="mt-6 p-4 bg-white/5 rounded-lg">
        <h3 class="text-lg font-bold text-white mb-2">💡 Conseils d'Expert :</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
          <div>
            <span class="text-yellow-400 font-semibold">🎵 Diversifiez :</span>
            <span class="text-gray-300">Jouez à différents types de playlists</span>
          </div>
          <div>
            <span class="text-green-400 font-semibold">⏰ Régularité :</span>
            <span class="text-gray-300">Connectez-vous quotidiennement</span>
          </div>
          <div>
            <span class="text-blue-400 font-semibold">🎯 Objectifs :</span>
            <span class="text-gray-300">Fixez-vous des objectifs quotidiens</span>
          </div>
          <div>
            <span class="text-purple-400 font-semibold">🏆 Excellence :</span>
            <span class="text-gray-300">Visez les meilleurs scores</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .reward-card {
    border: 2px solid transparent;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
  }
  
  .reward-card:hover {
    border-color: #8b5cf6;
    box-shadow: 0 20px 25px -5px rgba(139, 92, 246, 0.1), 0 10px 10px -5px rgba(139, 92, 246, 0.04);
    transform: translateY(-5px);
  }
  
  @keyframes gradient {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }
</style> 