<div class="min-h-screen bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900">
  <div class="container mx-auto px-4 py-8">
    <!-- Header avec logo -->
    <div class="text-center mb-8">
      <div class="flex items-center justify-center mb-6">
        <div class="w-16 h-16 bg-white/10 backdrop-blur-sm rounded-full flex items-center justify-center">
          <div class="w-8 h-8 text-purple-400">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
              <path d="M5 8h2v8H5zm7 0H9v8h3zm5 8h2V8h-2zM8 5h8v2H8z"/>
            </svg>
          </div>
        </div>
        <h1 class="text-4xl font-bold text-white ml-4">🏆 Toutes les Récompenses</h1>
      </div>
      <p class="text-gray-300 text-lg">Découvrez tous les badges et leurs récompenses disponibles</p>
    </div>

    <!-- Navigation moderne -->
    <div class="flex justify-center mb-8">
      <div class="flex space-x-2">
        <%= link_to "Mes récompenses", my_rewards_path, 
            class: "px-4 py-2 rounded-lg text-sm font-medium text-gray-300 bg-gray-700 hover:bg-gray-600 transition-colors" %>
        <%= link_to "Toutes les récompenses", all_rewards_path, 
            class: "px-4 py-2 rounded-lg text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 transition-colors" %>
      </div>
    </div>

    <!-- Statistiques globales -->
    <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 mb-8">
      <h2 class="text-2xl font-bold text-white mb-4">📊 Statistiques globales</h2>
      <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
        <div class="bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg p-4 text-center">
          <div class="text-3xl font-bold text-white"><%= Badge.count %></div>
          <div class="text-sm text-gray-200">Total badges</div>
        </div>
        <div class="bg-gradient-to-r from-green-500 to-emerald-600 rounded-lg p-4 text-center">
          <div class="text-3xl font-bold text-white"><%= @unlocked_rewards %></div>
          <div class="text-sm text-gray-200">Récompenses débloquées</div>
        </div>
        <div class="bg-gradient-to-r from-yellow-500 to-orange-600 rounded-lg p-4 text-center">
          <div class="text-3xl font-bold text-white"><%= @locked_rewards %></div>
          <div class="text-sm text-gray-200">Récompenses à débloquer</div>
        </div>
        <div class="bg-gradient-to-r from-purple-500 to-pink-600 rounded-lg p-4 text-center">
          <div class="text-3xl font-bold text-white"><%= User.count %></div>
          <div class="text-sm text-gray-200">Utilisateurs</div>
        </div>
      </div>
    </div>

    <!-- Toutes les récompenses par type -->
    <div class="mb-8">
      <h2 class="text-2xl font-bold text-white mb-6">🎯 Toutes les récompenses par type</h2>
      
      <% Badge.distinct.pluck(:badge_type).each do |badge_type| %>
        <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 mb-6">
          <h3 class="text-xl font-bold text-white mb-4">
            Récompenses <%= badge_type.humanize %>
            <span class="text-sm font-normal text-gray-300">
              (<%= @badge_counts[badge_type] || 0 %> badges collectés)
            </span>
          </h3>
          
          <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <% [3, 6, 9].each do |quantity| %>
              <% current_points = Reward.calculate_badge_points(current_user, badge_type) %>
              <% progress = [(current_points.to_f / quantity * 100), 100].min %>
              
              <%# Récupérer un exemple de badge pour ce type %>
              <% example_badge = Badge.where(badge_type: badge_type).first %>
              
              <div class="reward-card bg-gradient-to-r from-gray-700 to-gray-800 rounded-lg overflow-hidden border border-gray-600 transform hover:scale-105 transition-all duration-200">
                <!-- Bannière avec image -->
                <div class="h-48 relative">
                  <% if example_badge&.image.present? %>
                    <%= image_tag "badges/#{example_badge.image}", 
                        class: "w-full h-full object-cover",
                        alt: "#{badge_type.humanize} badge" %>
                  <% else %>
                    <div class="w-full h-full bg-gradient-to-r from-blue-500 to-purple-600 flex items-center justify-center">
                      <div class="text-white text-4xl">
                        <%= case quantity
                            when 3 then '🎯'
                            when 6 then '⭐'
                            when 9 then '👑'
                            end %>
                      </div>
                    </div>
                  <% end %>
                  <div class="absolute top-3 left-3">
                    <span class="bg-white/20 text-white text-xs px-2 py-1 rounded-full">NEW!</span>
                  </div>
                </div>
                
                <!-- Contenu de la carte -->
                <div class="p-4">
                  <!-- En-tête avec quantité -->
                  <div class="text-center mb-4">
                    <div class="text-3xl font-bold text-white mb-2"><%= quantity %></div>
                    <div class="text-sm text-gray-300">points requis</div>
                  </div>
                  
                  <!-- Type de récompense -->
                  <div class="text-center mb-4">
                    <div class="text-lg font-semibold text-white mb-1">
                      <%= case quantity
                          when 3 then '🎯 Challenge'
                          when 6 then '⭐ Exclusif'
                          when 9 then '👑 Premium'
                          end %>
                    </div>
                    <div class="text-xs text-gray-400">
                      <%= case quantity
                          when 3 then 'Défi spécial'
                          when 6 then 'Contenu exclusif'
                          when 9 then 'Récompense premium'
                          end %>
                    </div>
                  </div>
                  
                  <!-- Progression -->
                  <div class="mb-4">
                    <div class="flex justify-between text-xs text-gray-400 mb-2">
                      <span>Progression</span>
                      <span><%= current_points %>/<%= quantity %></span>
                    </div>
                    <div class="w-full bg-gray-600 rounded-full h-2">
                      <div class="bg-gradient-to-r from-blue-500 to-purple-600 h-2 rounded-full transition-all duration-300" 
                           style="width: <%= progress %>%"></div>
                    </div>
                  </div>
                  
                  <!-- Statut et lien détails -->
                  <div class="flex items-center justify-between">
                    <div class="text-center">
                      <% if current_points >= quantity %>
                        <div class="text-green-400 text-sm font-medium">
                          ✅ Prêt à débloquer !
                        </div>
                      <% else %>
                        <div class="text-gray-500 text-sm">
                          🔒 Il manque <%= quantity - current_points %> point(s)
                        </div>
                      <% end %>
                    </div>
                    <%= link_to reward_details_path(badge_type: badge_type, quantity: quantity), 
                        class: "text-blue-400 hover:text-blue-300 text-sm flex items-center" do %>
                      Détails →
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>

    <!-- Guide des récompenses -->
    <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6">
      <h2 class="text-2xl font-bold text-white mb-4">📖 Guide des récompenses</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="text-center">
          <div class="text-4xl mb-2">🎯</div>
          <h3 class="text-lg font-bold text-white mb-2">Challenge (3 badges)</h3>
          <p class="text-gray-300 text-sm">Collectez 3 badges du même type pour débloquer des défis spéciaux</p>
        </div>
        <div class="text-center">
          <div class="text-4xl mb-2">⭐</div>
          <h3 class="text-lg font-bold text-white mb-2">Exclusif (5 badges)</h3>
          <p class="text-gray-300 text-sm">Collectez 5 badges du même type pour accéder à du contenu exclusif</p>
        </div>
        <div class="text-center">
          <div class="text-4xl mb-2">👑</div>
          <h3 class="text-lg font-bold text-white mb-2">Premium (10 badges)</h3>
          <p class="text-gray-300 text-sm">Collectez 10 badges du même type pour débloquer des récompenses premium</p>
        </div>
      </div>
      
      <div class="mt-6 p-4 bg-white/5 rounded-lg">
        <h3 class="text-lg font-bold text-white mb-2">Types de badges disponibles :</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
          <div>
            <span class="text-blue-400 font-semibold">Competitor :</span>
            <span class="text-gray-300">Badges basés sur les points gagnés</span>
          </div>
          <div>
            <span class="text-green-400 font-semibold">Engager :</span>
            <span class="text-gray-300">Badges basés sur l'engagement (likes/dislikes)</span>
          </div>
          <div>
            <span class="text-red-400 font-semibold">Critic :</span>
            <span class="text-gray-300">Badges basés sur les critiques</span>
          </div>
          <div>
            <span class="text-purple-400 font-semibold">Challenger :</span>
            <span class="text-gray-300">Badges basés sur la performance globale</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .reward-card {
    border: 2px solid transparent;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
  }
  
  .reward-card:hover {
    border-color: #8b5cf6;
    box-shadow: 0 20px 25px -5px rgba(139, 92, 246, 0.1), 0 10px 10px -5px rgba(139, 92, 246, 0.04);
    transform: translateY(-5px);
  }
  
  @keyframes gradient {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }
</style> 