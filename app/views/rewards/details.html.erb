<div class="min-h-screen bg-gray-900 py-4 sm:py-8">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- En-tête -->
    <div class="text-center mb-8">
      <div class="flex items-center justify-center mb-6">
        <div class="w-16 h-16 bg-gray-800 rounded-full flex items-center justify-center">
          <div class="w-8 h-8 text-cyan-400">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
            </svg>
          </div>
        </div>
        <h1 class="text-4xl font-bold text-white ml-4"><%= @reward_name %></h1>
      </div>
    </div>

    <!-- Bouton retour -->
    <div class="mb-8">
      <%= link_to "← Retour aux récompenses", all_rewards_path, 
          class: "text-blue-400 hover:text-blue-300 font-medium" %>
    </div>

    <!-- Détails de la récompense -->
    <div class="max-w-2xl mx-auto">
      <div class="bg-white/10 backdrop-blur-sm rounded-xl p-8 shadow-lg">
        <div class="flex items-center justify-between mb-6">
          <div class="text-4xl">
            <%= case @quantity
                when 3 then '🎯'
                when 6 then '⭐'
                when 9 then '👑'
                end %>
          </div>
          <div class="text-right">
            <% if @current_count && @current_count >= @quantity %>
              <span class="inline-block px-3 py-1 rounded-full text-sm font-medium bg-green-500 text-white">
                DÉBLOQUÉ
              </span>
            <% else %>
              <span class="inline-block px-3 py-1 rounded-full text-sm font-medium bg-gray-500 text-white">
                VERROUILLÉ
              </span>
            <% end %>
          </div>
        </div>

        <!-- Image du badge -->
        <% example_badge = Badge.where(badge_type: @badge_type).first %>
        <% if example_badge&.image.present? %>
          <div class="text-center mb-6">
            <%= image_tag "badges/#{example_badge.image}", 
                class: "w-48 h-48 object-cover rounded-lg shadow-lg",
                alt: "#{@badge_type.humanize} badge" %>
          </div>
        <% end %>

        <h2 class="text-2xl font-bold text-white mb-4">
          <%= @reward_name %> - <%= @badge_type.humanize %>
        </h2>

        <div class="space-y-4">
          <div>
            <h3 class="text-lg font-semibold text-white mb-2">Description</h3>
            <p class="text-gray-300"><%= @reward_description %></p>
          </div>

          <div class="grid grid-cols-2 gap-4">
            <div>
              <h3 class="text-lg font-semibold text-white mb-2">Type de Récompense</h3>
              <p class="text-gray-300"><%= @badge_type.humanize %></p>
            </div>
            <div>
              <h3 class="text-lg font-semibold text-white mb-2">Quantité requise</h3>
              <p class="text-gray-300"><%= @quantity %> badges</p>
            </div>
          </div>

          <!-- Progression actuelle -->
          <div>
            <h3 class="text-lg font-semibold text-white mb-2">Votre progression</h3>
            <div class="flex items-center justify-between mb-2">
              <span class="text-gray-300">
                <%= @current_count || 0 %> / <%= @quantity %>
              </span>
              <span class="text-gray-300">
                <%= @progress.to_i %>%
              </span>
            </div>
            
            <div class="w-full bg-gray-600 rounded-full h-3">
              <div class="bg-gradient-to-r from-blue-500 to-purple-600 h-3 rounded-full transition-all duration-300" 
                   style="width: <%= @progress %>%"></div>
            </div>
            
            <!-- Détails par niveau -->
            <div class="mt-4 space-y-2">
              <div class="flex justify-between items-center">
                <span class="text-gray-300">🥉 Bronze</span>
                <span class="text-gray-300"><%= @bronze_count %> badge(s)</span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-gray-300">🥈 Silver</span>
                <span class="text-gray-300"><%= @silver_count %> badge(s)</span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-gray-300">🥇 Gold</span>
                <span class="text-gray-300"><%= @gold_count %> badge(s)</span>
              </div>
              <div class="border-t border-gray-600 pt-2 mt-2">
                <div class="flex justify-between items-center font-semibold">
                  <span class="text-white">Total badges</span>
                  <span class="text-white"><%= @current_count || 0 %></span>
                </div>
              </div>
            </div>
            
            <% if @current_count && @current_count >= @quantity %>
              <p class="text-green-400 text-sm mt-2">✅ Vous avez assez de badges pour débloquer cette récompense !</p>
            <% else %>
              <p class="text-gray-400 text-sm mt-2">
                Il vous manque <%= @quantity - (@current_count || 0) %> badge(s) pour débloquer cette récompense
              </p>
            <% end %>
          </div>
        </div>

        <!-- Actions -->
        <div class="mt-8 pt-6 border-t border-gray-600">
          <% if @current_count && @current_count >= @quantity %>
            <div class="text-center">
              <p class="text-green-400 font-medium">🎉 Cette récompense est prête à être débloquée !</p>
            </div>
          <% else %>
            <div class="text-center">
              <p class="text-gray-400 mb-4">Collectez plus de badges <%= @badge_type.humanize %> pour débloquer cette récompense</p>
              <%= link_to "Jouer pour gagner des badges", playlists_path, 
                  class: "inline-block bg-gradient-to-r from-blue-500 to-purple-600 text-white font-bold py-3 px-6 rounded-lg shadow-lg transform hover:scale-105 transition-all duration-200" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div> 